# The static site uses no secrets at runtime. These names document the
# related service configuration kept OUTSIDE this repo (never commit values).
# Real values live in C:\Users\daren\ai-assistant\.env on the owner's machine.

# Telephony (demo line + SMS summaries)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Voice agent platform
ELEVENLABS_API_KEY=

# Scheduling (booking link used on book.html)
CALCOM_API_KEY=
CALCOM_EVENT_TYPE_ID=
PUBLIC_BOOKING_URL=https://cal.com/daren-qvlah4/nevamis-intro

# Payments (invoicing / future checkout)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Analytics (add provider snippet per README; no key needed for the event layer)
