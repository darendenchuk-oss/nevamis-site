# Technical & On-Page SEO — 65 Improvements

> **SUPERSEDED 2026-08-09 — the commercial model this file was written against no longer exists.**
> Ideas below were authored while Nevamis sold the C$249 / C$449 / C$849 ladder (plans named
> After Hours, Growth and Scale), a Pay As You Go tier at C$49 + C$1.95/min, annual prepay, a
> setup fee with a founding-client waiver, and a 7-day live pilot — free at first, then C$150.
> Every one of those is retired. The current model is ONE recurring price per plan, charged the
> day the client subscribes and every month after: **Core C$250/month · Growth C$500/month · Pro
> C$1,000/month**, with 250 / 600 / 1,400 included minutes and C$1.10 / C$0.90 / C$0.75 overage,
> nothing charged beside it, and no pilot or trial at any price. `pricing-config.js` is the source
> of truth; docs/CLAIMS-LEDGER.md row CLM-18 is the approval.
>
> The ideas are kept rather than deleted: most are about how a price is *presented*, and that work
> survives the change. But no figure, plan name or offer quoted below may be copied onto a surface,
> and any idea whose whole premise is a setup fee, a pilot, PAYG or annual prepay is moot.


Nevamis has a genuinely good technical foundation for a solo-founder static site: eleven hand-written pages on GitHub Pages, per-page canonicals, `en-CA` lang, self-hosted woff2 fonts, a git-derived `sitemap.xml` generator, a `check-consistency.js` guard, `llms.txt`, and 37 Playwright tests. What it does not yet have is schema beyond `Organization`/`WebSite`/`BreadcrumbList`, a heading hierarchy that survives a crawl (pricing.html goes `h1` straight to `h3`), pricing markup that exists without JavaScript on the homepage, or a Core Web Vitals story — the full-screen opaque `#wake` overlay in `home.html` almost certainly delays LCP, and three internal design comps plus a 1.3 MB unused `three.module.js` are published live at `nevamis.ca/concept.html` and `nevamis.ca/brand/`. The list below is ordered roughly schema → on-page → linking → crawl → performance → images → Search Console. Every item is grounded in the files as they actually exist on 2026-07-27; nothing here requires a client count, a testimonial, a response-time statistic, or any other claim the business cannot support today.

---

1. **SEO-TECHNICAL-ONPAGE-001 — Emit FAQPage schema from the 14 existing `<details>` blocks on the homepage.** `home.html` lines 740–753 already contain fourteen owner-grade Q&As ("Will callers know it is AI?", "Can I keep my current phone number?", "Is the 7-day live pilot actually free?") in `<details><summary>` markup, and none of it is machine-readable. Add a second `application/ld+json` block with `@type: FAQPage` whose `mainEntity` array mirrors the visible text verbatim — Google's policy requires exact parity, so generate it with a small script rather than retyping. FAQ rich results are now limited for commercial sites, but the same markup is what Perplexity, ChatGPT Search and AI Overviews parse when answering "does an AI receptionist keep my existing number".
   impact 4/5 · effort 2/5 · touches: home.html, scripts/ (new gen-faq-schema.mjs), then promote.mjs

2. **SEO-TECHNICAL-ONPAGE-002 — Add the missing BreadcrumbList to pricing.html, privacy.html and terms.html.** Six pages (pilot, demo, book, about, coming-soon, revenue-engine) each carry a two-item `BreadcrumbList`; `pricing.html`, `privacy.html` and `terms.html` carry zero `application/ld+json` blocks at all. Copy the exact pattern from `pilot.html` lines 16–20 with `name` values "Pricing", "Privacy", "Terms". Pricing is the second-most commercially important page on the site and is currently the only one of the three that will ever earn a SERP breadcrumb.
   impact 2/5 · effort 1/5 · touches: pricing.html, privacy.html, terms.html

3. **SEO-TECHNICAL-ONPAGE-003 — Consolidate all structured data into a single `@graph` with stable `@id` anchors.** Right now `home.html` declares a full `Organization` object and every subpage declares an orphan `BreadcrumbList` that references nothing. Rewrite as one `@graph` per page containing `{"@id":"https://nevamis.ca/#organization"}` (defined in full only on `/`), `{"@id":"https://nevamis.ca/#website"}`, a `WebPage` node with `isPartOf` and `about` pointing at those `@id`s, and the breadcrumb. This is what lets Google treat all eleven pages as one entity instead of eleven unrelated documents, and it removes the temptation to duplicate the Organization block eleven times and let it drift.
   impact 3/5 · effort 3/5 · touches: all 10 indexable .html files, docs/CLAIMS-LEDGER.md

4. **SEO-TECHNICAL-ONPAGE-004 — Add a `Service` node describing the AI receptionist itself.** The Organization block says who Nevamis is but nothing says what is sold. On `index.html` add `{"@type":"Service","name":"AI Receptionist","serviceType":"Answering service","provider":{"@id":"…#organization"},"areaServed":[{"@type":"City","name":"Edmonton"},{"@type":"AdministrativeArea","name":"Alberta"},{"@type":"Country","name":"Canada"}],"description":"…"}` with `availableChannel` pointing at `tel:+15874130035`. Use the exact wording from the hero lede so the schema and the visible copy cannot diverge.
   impact 3/5 · effort 2/5 · touches: home.html

5. **SEO-TECHNICAL-ONPAGE-005 — Generate `Offer` schema for the three plans directly from pricing-config.js.** The plan numbers already live in one approved file (`pricing-config.js`: After Hours 249/500, Growth 449/750, Scale 849/1250). Extend `scripts/check-consistency.js` or add `scripts/gen-offer-schema.mjs` that reads `NV_PRICING` and writes an `OfferCatalog` into `pricing.html` with `priceCurrency:"CAD"`, `price`, `unitText:"MONTH"`, `eligibleRegion:"CA"` and a `priceValidUntil` twelve months out (matching the "prices locked 12 months" claim in `llms.txt`). Generated-not-typed is the whole point: pricing can never silently drift out of the markup.
   impact 4/5 · effort 3/5 · touches: pricing.html, pricing-config.js, scripts/

6. **SEO-TECHNICAL-ONPAGE-006 — Add a `Person` node for Daren on about.html and link it as `founder`.** `about.html` is the trust page and `home.html` line 706 already ships `assets/daren.jpg` with a proper alt. Add `{"@type":"Person","name":"Daren Denchuk","jobTitle":"Founder","worksFor":{"@id":"…#organization"},"image":"https://nevamis.ca/assets/daren.jpg"}` and reference it from the Organization as `founder`. Once a LinkedIn profile exists, add it to `sameAs` — do not add a `sameAs` array full of empty or unclaimed profiles.
   impact 2/5 · effort 1/5 · touches: about.html, home.html

7. **SEO-TECHNICAL-ONPAGE-007 — Populate `dateModified` on every page from git, the same way sitemap lastmod already works.** `scripts/gen-sitemap.mjs` already runs `git log -1 --format=%cs` per file to derive `lastmod`. Reuse that function to inject `"dateModified"` into each page's `WebPage` node and keep it in lockstep with the sitemap, so the two signals never contradict each other. Contradictory freshness signals are a known reason crawlers discount `lastmod` entirely.
   impact 2/5 · effort 2/5 · touches: scripts/gen-sitemap.mjs, all indexable .html

8. **SEO-TECHNICAL-ONPAGE-008 — Declare the demo line's real 24/7 availability with `hoursAvailable`.** The `ContactPoint` in `home.html` lists `+1-587-413-0035` with `contactType:"sales"` but no hours, while the whole pitch is "answers any hour". Add `"hoursAvailable":{"@type":"OpeningHoursSpecification","dayOfWeek":["Monday",…,"Sunday"],"opens":"00:00","closes":"23:59"}`. This is a claim the business can support — the demo agent genuinely does answer at 3am — and it is the single most differentiating attribute versus a human answering service.
   impact 2/5 · effort 1/5 · touches: home.html

9. **SEO-TECHNICAL-ONPAGE-009 — Mark up the eleven example-call MP3s as an `AudioObject` that carries the fiction disclosure in the data.** `assets/call-0.mp3` through `call-10.mp3` are real audio files of a scripted Cedarview Electric call, and the page already footnotes "Illustrative call, fictional company. Real AI voices." Add an `AudioObject` with `name:"Example AI receptionist call (illustrative)"`, `encodingFormat:"audio/mpeg"`, `transcript` containing the visible transcript, and `disambiguatingDescription` repeating the fiction disclosure so no LLM can scrape the transcript and present it as a real customer.
   impact 2/5 · effort 2/5 · touches: home.html

10. **SEO-TECHNICAL-ONPAGE-010 — Mark the six-step "how a missed call becomes a booked job" list as an `ItemList`, not `HowTo`.** `home.html` `#how` (lines 445–450) is a clean six-`<li>` sequence with `<h3>` per step. Google retired HowTo rich results in 2023, so do not chase that; use `{"@type":"ItemList","itemListOrder":"Ascending","numberOfItems":6}` with each step's heading and paragraph. The value here is purely machine comprehension for AI answer engines summarising "how does an AI receptionist work" — say so in the code comment so nobody later rips it out expecting SERP stars.
   impact 2/5 · effort 2/5 · touches: home.html

11. **SEO-TECHNICAL-ONPAGE-011 — Decide the LocalBusiness question honestly, then act on it once.** Every page says "Edmonton, Alberta, Canada" and the Organization `PostalAddress` deliberately has no `streetAddress`. `LocalBusiness` / `ProfessionalService` schema and a Google Business Profile are the single biggest local-SEO lever for "AI receptionist Edmonton" style queries, but both need a real, verifiable address (a home address with the "service area business" hide-address option is legitimate; a fake or virtual-office address is not). Make the call this week: either publish a real service-area address and upgrade the schema to `ProfessionalService` with `areaServed` radius, or accept national-only targeting and stop half-declaring locality.
   impact 4/5 · effort 3/5 · touches: home.html, about.html, Google Business Profile

12. **SEO-TECHNICAL-ONPAGE-012 — Add tests/seo.spec.js that parses every `ld+json` block and fails the build on invalid or drifted schema.** The repo has `tests/motion.spec.js`, `interactions.spec.js` and `pages.spec.js` but nothing asserting metadata. Add a spec that, for each page, asserts: exactly one canonical and it matches the URL, `robots` is `index, follow` (except 404 and home.html), title length 15–60 chars, description 70–160 chars, every `ld+json` block is valid JSON, and every FAQ `mainEntity.name` appears verbatim in the rendered DOM. This turns SEO from a thing that decays into a thing that breaks the test run.
   impact 4/5 · effort 3/5 · touches: tests/seo.spec.js (new), playwright.config.js

13. **SEO-TECHNICAL-ONPAGE-013 — Extend the banned-phrase guard in check-consistency.js to ban fabricated schema types.** `scripts/check-consistency.js` already blocks `/join thousands/i`, `/free trial/i`, `/\$397\b/` and friends in page copy. Add a second regex set that fails on `"@type":"AggregateRating"`, `"@type":"Review"`, `"reviewCount"`, `"ratingValue"`, and `"potentialAction":{"@type":"SearchAction"` (there is no site search) anywhere in the HTML. With zero clients, the temptation to paste a "5.0 from 27 reviews" snippet from a schema generator is exactly the failure mode worth hard-coding against.
   impact 3/5 · effort 1/5 · touches: scripts/check-consistency.js

14. **SEO-TECHNICAL-ONPAGE-014 — Record one real 60–90 second demo capture and give it VideoObject markup.** There is currently no video anywhere on the site; the closest thing is the MP3 transcript player. A single screen-and-audio capture of a live call into (587) 413-0035 — dial, the AI answers, a booking happens, the SMS lands — hosted as an MP4 in `/assets/` and embedded on `demo.html` earns a `VideoObject` with `name`, `uploadDate`, `duration`, `thumbnailUrl` and `contentUrl`, which is one of the few rich-result types still reliably granted. It is also the highest-leverage sales asset on this list.
   impact 4/5 · effort 4/5 · touches: demo.html, assets/, sitemap.xml

15. **SEO-TECHNICAL-ONPAGE-015 — Add `sameAs` to the Organization node the day each profile actually exists, and only then.** The Organization block has no `sameAs` array. Keep a one-line checklist in `docs/CLAIMS-LEDGER.md` covering Google Business Profile, LinkedIn company page, and a Bing Places listing, and add each URL to `sameAs` at the moment it is verified. Entity consolidation is cheap and permanent, but an array of dead or unowned URLs is a negative signal.
   impact 2/5 · effort 1/5 · touches: home.html, docs/CLAIMS-LEDGER.md

16. **SEO-TECHNICAL-ONPAGE-016 — Rewrite pricing.html's title and description, which are the thinnest on the site.** They currently read `<title>Pricing | Nevamis AI</title>` and `"Nevamis AI receptionist plans and what each includes."` — 20 and 51 characters, wasting the entire SERP snippet on a page where the numbers are the hook. Replace with `AI Receptionist Pricing in Canada — from C$249/mo | Nevamis AI` and `AI receptionist plans in Canadian dollars: After Hours C$249/mo, Growth C$449/mo, Scale from C$849/mo. Month to month, plus a free 7-day live pilot with no card.` Every figure there is already published on the page, so nothing new is being claimed.
   impact 4/5 · effort 1/5 · touches: pricing.html

17. **SEO-TECHNICAL-ONPAGE-017 — Fix the broken heading hierarchy on pricing.html: it jumps h1 → h3 with no h2 anywhere.** `grep '<h2' pricing.html` returns nothing; the page goes from "Clear plans. Predictable usage." (h1, line 85) straight to `<h3>After Hours — C$249/month</h3>` and `<h3>How usage works</h3>`. Wrap the plan block in a section led by `<h2>The three plans</h2>` and give the recommender its own `<h2>Which plan fits your call volume?</h2>`. This is both an accessibility defect and the reason the page's topical structure is invisible to a crawler.
   impact 3/5 · effort 1/5 · touches: pricing.html

18. **SEO-TECHNICAL-ONPAGE-018 — Front-load the homepage title instead of leading with the brand nobody searches for.** It is currently `Nevamis AI | AI Receptionists for Canadian Businesses | Edmonton` — the first 11 characters are spent on a brand with zero search volume today. Flip to `AI Receptionist for Canadian Trades — Answers 24/7 | Nevamis AI` and keep "Edmonton" in the h1 eyebrow and description where it already sits. Brand-first titles are correct for known brands; for a pre-revenue business they burn the only pixels that matter.
   impact 3/5 · effort 1/5 · touches: home.html, then scripts/promote.mjs

19. **SEO-TECHNICAL-ONPAGE-019 — Write real descriptions for privacy.html, terms.html and 404.html.** Privacy is 55 characters ("What the Nevamis AI website collects and how it is used."), terms is similar, and `404.html` has no `<meta name="description">` at all. These pages will never rank, but a trades owner who Googles "nevamis privacy" or lands on a broken link sees the snippet, and a 60-word description costs five minutes. Keep 404 `noindex` as-is.
   impact 1/5 · effort 1/5 · touches: privacy.html, terms.html, 404.html

20. **SEO-TECHNICAL-ONPAGE-020 — Resolve the three-way h1 collision between the homepage, coming-soon.html and revenue-engine.html.** `revenue-engine.html` h1 is "The front desk is where we start.", `coming-soon.html` h1 is "The front desk is only the beginning.", and `home.html` `#beyond` uses "The front desk is where we start." as an h2 — three near-identical strings competing on the same theme. Either merge `revenue-engine.html` into `coming-soon.html` with a 301-equivalent canonical (`<link rel="canonical" href="https://nevamis.ca/coming-soon.html">` plus `noindex` on the loser), or give each a distinct angle: coming-soon = the product roadmap, revenue-engine = the measurement layer. Leaving both indexable as-is guarantees they split each other's signals.
   impact 3/5 · effort 2/5 · touches: revenue-engine.html, coming-soon.html, home.html, sitemap.xml

21. **SEO-TECHNICAL-ONPAGE-021 — Demote the footer's `<h4>` column labels out of the heading outline.** "Nevamis AI", "Site" and "Legal" are `<h4>` elements repeated on every full-footer page, so every document's heading outline ends with three content-free headings. Change them to `<p class="foot-label">` and put the semantics where they belong: `<nav aria-label="Site">` and `<nav aria-label="Legal">`. Update `check-consistency.js`, whose `siteColOf()` currently matches on `<h4>Site</h4>`.
   impact 2/5 · effort 2/5 · touches: all full-footer pages, assets/motion/site.css, scripts/check-consistency.js

22. **SEO-TECHNICAL-ONPAGE-022 — Give the punchiest h2s a keyword-bearing supporting line.** Headings like "Your customers call. Your competitors answer." (`#industries`) and "Voicemail records the lost job. Nevamis books it." (`#compare`) are strong copy and terrible retrieval targets. Do not weaken them — add the existing `<p>` beneath each to carry the plain phrasing an owner would type: "24/7 phone answering for Edmonton electricians, HVAC, plumbing and restoration crews". The `.section-head` pattern already has a paragraph slot in every section, so this is copy edits only.
   impact 3/5 · effort 2/5 · touches: home.html (#industries, #compare, #solutions, #how)

23. **SEO-TECHNICAL-ONPAGE-023 — Add the missing Twitter card fields and `og:image:alt` sitewide.** Every page ships `twitter:card` and `twitter:image` but no `twitter:title` or `twitter:description`, and no `og:image:alt` anywhere. Twitter/X falls back to OG in most cases, but Slack, LinkedIn and iMessage previews — which is where a strategy-call link actually gets pasted — render inconsistently without them. Add all three to the shared head block on all ten indexable pages.
   impact 2/5 · effort 1/5 · touches: all indexable .html

24. **SEO-TECHNICAL-ONPAGE-024 — Build three per-page OG images instead of reusing og-default.png everywhere.** All ten pages point `og:image` at the same `assets/og-default.png`. Generate variants for `/pricing.html` (the three plan prices), `/pilot.html` ("7 days. No card.") and `/demo.html` (the phone number, large) from the existing `assets/og-default.svg` source — it is a 1.5 KB SVG, so three copies is a text edit plus an export. When Daren pastes a pricing link into a contractor Facebook group, the preview should be the offer, not the logo.
   impact 3/5 · effort 2/5 · touches: assets/og-default.svg, assets/, pricing.html, pilot.html, demo.html

25. **SEO-TECHNICAL-ONPAGE-025 — Add `theme-color` and `color-scheme` meta tags.** The site is committed dark navy (`--navy-0`) but declares neither, so mobile Chrome paints a white browser chrome above a black page and the first paint flashes white. Add `<meta name="theme-color" content="#0B1620">` and `<meta name="color-scheme" content="dark">` to every page head. This is a perceived-performance and polish fix that costs one line.
   impact 2/5 · effort 1/5 · touches: all .html

26. **SEO-TECHNICAL-ONPAGE-026 — Normalise the title/description/OG triad so the three never contradict each other.** `home.html` currently has `<title>` "…AI Receptionists for Canadian Businesses | Edmonton" but `og:title` "Stop losing jobs to missed calls." — deliberate and fine — while `pricing.html`, `privacy.html` and `terms.html` have no `og:title`/`og:description` at all and inherit nothing. Define the rule explicitly in `README.md`: title = search intent, og:title = emotional hook, description = the offer with a number, and every page must declare all four. Then enforce it in the new `tests/seo.spec.js`.
   impact 2/5 · effort 2/5 · touches: pricing.html, privacy.html, terms.html, README.md, tests/seo.spec.js

27. **SEO-TECHNICAL-ONPAGE-027 — Assert title and description uniqueness across the site in check-consistency.js.** The script already enforces one shared nav, one shared footer column, and banned phrases across ten content pages; it does not check that no two pages share a `<title>` or `<meta name="description">`. Add a rule that collects both from `contentPages` and fails on any duplicate, with `home.html`/`index.html` whitelisted as the intentional staging twin. Duplicate titles are the most common way a hand-maintained static site degrades as pages get copy-pasted.
   impact 3/5 · effort 1/5 · touches: scripts/check-consistency.js

28. **SEO-TECHNICAL-ONPAGE-028 — Put the phone number in the h1 or first paragraph of demo.html, not only the title.** The title is already `Live Demo | Call the Nevamis AI Receptionist | (587) 413-0035`, but the h1 is just "Try to stump it." Number-in-h1 pages win "call an AI receptionist demo" style queries and, more importantly, an AI answer engine summarising the page can only cite the number if it appears in body text near a heading. Add it to the sub-lede as visible, linked text: `<a href="tel:+15874130035">(587) 413-0035</a>`.
   impact 2/5 · effort 1/5 · touches: demo.html

29. **SEO-TECHNICAL-ONPAGE-029 — Link the homepage `#beyond` section to /revenue-engine.html, which currently has no inbound internal link outside the sitemap.** The three "PLANNED" cards (Instant Lead Follow-Up, Automatic Lead Tracking, Quote Recovery) at `home.html` 722–724 describe exactly what `/revenue-engine.html` covers, and neither the nav nor the footer nor the section links to it — only `/coming-soon.html` is linked. Add a single contextual link under the cards. An orphaned page on a ten-page site is a page Google will crawl last and rank never.
   impact 3/5 · effort 1/5 · touches: home.html

30. **SEO-TECHNICAL-ONPAGE-030 — Turn the four `#industries` cards into real anchors.** They are currently `<div class="ind-card" data-tilt>` with no link target, which means the strongest topical block on the homepage passes no internal signal anywhere. Even before dedicated industry pages exist, point them at the relevant homepage anchors or at `/book.html?industry=electrical`, with descriptive anchor text on the `<h3>`. Keep the tilt effect by making the `<h3>` contents the anchor rather than wrapping the card.
   impact 3/5 · effort 2/5 · touches: home.html, assets/motion/site.css

31. **SEO-TECHNICAL-ONPAGE-031 — Build four industry landing pages as the site's first real organic bet.** `/ai-receptionist-for-electricians.html`, `-hvac-plumbing`, `-restoration`, `-automotive`, each with its own title/description/canonical, a `BreadcrumbList` (Home › Industries › Electricians), a `Service` node with `serviceType`, the existing `assets/industries/*.webp` art as a real `<img>`, industry-specific FAQ, and a sitemap entry. Reuse the `page-hero` + `tight` section pattern from `pilot.html` so there is no new CSS. These are the pages that can actually rank for "after hours answering service for electricians Edmonton" — the homepage never will.
   impact 5/5 · effort 4/5 · touches: 4 new .html files, sitemap.xml, scripts/gen-sitemap.mjs, home.html

32. **SEO-TECHNICAL-ONPAGE-032 — Add contextual links inside the remaining FAQ answers.** Three of the fourteen answers already link out (to `/pricing.html` and `/pilot.html`); eleven do not. "Can it book directly into my calendar?" should link to `/book.html`, "Will it sound robotic?" to `/demo.html`, "How is customer information handled?" to `/privacy.html`, "How long does setup take?" to the `#first-week` anchor. FAQ answers are the highest-dwell block on the page and the cheapest internal-link real estate on the site.
   impact 2/5 · effort 1/5 · touches: home.html

33. **SEO-TECHNICAL-ONPAGE-033 — Render visible breadcrumbs on subpages to match the BreadcrumbList already in the markup.** Six pages declare a breadcrumb in JSON-LD that has no on-page counterpart; Google's guidance is that structured data should reflect visible content, and users on a deep-linked pricing page currently have only the header nav. Add a `<nav class="crumbs" aria-label="Breadcrumb">Home › Pricing</nav>` strip above each `page-hero` using the existing `.mono` eyebrow style. One CSS rule, one line per page.
   impact 2/5 · effort 2/5 · touches: 9 subpages, assets/motion/site.css

34. **SEO-TECHNICAL-ONPAGE-034 — Make the pilot page the internal-link hub it deserves to be.** The free 7-day pilot is the wedge for the entire sales motion, yet `/pilot.html` is linked from the nav, the footer, and two FAQ answers — and nothing else. Add a contextual link from the homepage `#risk` section body copy, the pricing page's plan cards ("try any plan free for 7 days first"), and `demo.html`'s closing CTA. Internal link count is a ranking input, and it also just routes more traffic to the page that converts.
   impact 3/5 · effort 1/5 · touches: home.html, pricing.html, demo.html

35. **SEO-TECHNICAL-ONPAGE-035 — Add the missing pages to the 404 recovery links and confirm GitHub Pages returns a real 404 status.** `404.html` links to demo, pricing, pilot and book but not about, coming-soon or revenue-engine. More importantly, verify with `curl -I https://nevamis.ca/does-not-exist` that the response is HTTP 404 and not 200 — a soft 404 on a Pages site silently poisons the index-coverage report. Two-minute check, and it appears in Search Console as "Soft 404" if wrong.
   impact 2/5 · effort 1/5 · touches: 404.html

36. **SEO-TECHNICAL-ONPAGE-036 — Make anchor text descriptive where it currently is not.** The footer links "How It Works" and "Industries" point at `#how` and `#industries` on the homepage from every page, which is fine, but the homepage CTA row uses "Book a strategy call" seven times and the hero-links nav duplicates the header nav verbatim on mobile. Vary the inbound anchor text to pricing across pages ("what each plan includes", "AI receptionist pricing in CAD") rather than repeating "Pricing". Repeated identical anchors from the same template contribute nothing beyond the first.
   impact 2/5 · effort 2/5 · touches: home.html, pricing.html, pilot.html, demo.html

37. **SEO-TECHNICAL-ONPAGE-037 — Exclude `brand/` from the published site; it is currently live at nevamis.ca/brand/.** `_config.yml` excludes `docs/`, `config/`, `scripts/`, `tests/`, `artifacts/` and the working markdown, but not `brand/` — which contains `cover.html`, `logo.html`, `mark.html`, `mark2.html`, `mark-final.html`, `founder-sheet.html`, `photo-how.html` plus five PNGs, all publicly fetchable and none carrying `noindex`. Add `- brand/` to the exclude list. These are internal design comps; they are thin, duplicate-ish, and they dilute what Google understands the site to be about.
   impact 4/5 · effort 1/5 · touches: _config.yml

38. **SEO-TECHNICAL-ONPAGE-038 — Stop publishing the three concept pages and the 1.4 MB of vendor JS only they use.** `concept.html`, `concept-b.html` and `concept-c.html` (720 lines total) are internal design explorations; they carry `noindex, nofollow` so they will not be indexed, but they are still crawled, still served, and they are the only consumers of `assets/vendor/three.module.js` (1,314,681 bytes, unminified) and `assets/vendor/ScrollTrigger.min.js` (43 KB) — neither of which any live page loads. Add all three plus those two vendor files to `_config.yml` exclude. That is 1.4 MB removed from the deployed site and three fewer URLs of crawl budget spent on nothing.
   impact 3/5 · effort 1/5 · touches: _config.yml, assets/vendor/

39. **SEO-TECHNICAL-ONPAGE-039 — Tighten robots.txt beyond the current four lines.** It is currently `User-agent: * / Allow: / / Sitemap:` and nothing else. Add `Disallow: /ring.xml` (the Twilio routing document that exposes the ElevenLabs inbound endpoint — Twilio fetches it directly and does not consult robots), `Disallow: /brand/`, `Disallow: /concept` as belt-and-braces alongside the `_config.yml` change, and a comment pointing at `/llms.txt`. Keep `Allow: /` first so nothing else changes.
   impact 3/5 · effort 1/5 · touches: robots.txt

40. **SEO-TECHNICAL-ONPAGE-040 — Explicitly welcome the AI crawlers, since being cited by them is a stated goal.** `llms.txt` exists and is well written, but `robots.txt` says nothing about `GPTBot`, `ClaudeBot`, `PerplexityBot`, `Google-Extended`, `CCBot` or `Bingbot`. Add named `User-agent` blocks with `Allow: /` for each. Default-allow already permits them, but several of these crawlers and the compliance tooling around them check for an explicit block, and an explicit allow is a one-time decision the business should record rather than leave implied.
   impact 2/5 · effort 1/5 · touches: robots.txt

41. **SEO-TECHNICAL-ONPAGE-041 — Settle the extensionless-URL duplication that GitHub Pages creates.** Pages serves `about.html` at both `/about` and `/about.html`; every internal link and the sitemap use the `.html` form and every canonical matches, so the current state is safe — but it is safe by accident. Add a line to `README.md` naming `.html` as the canonical form, and add an assertion to `tests/seo.spec.js` that fetching `/pricing` returns a page whose canonical is `https://nevamis.ca/pricing.html`. Getting this wrong later (one extensionless link in an email campaign) is how duplicate-content reports start.
   impact 2/5 · effort 2/5 · touches: README.md, tests/seo.spec.js

42. **SEO-TECHNICAL-ONPAGE-042 — Confirm the www → apex redirect and HTTPS enforcement at the DNS and Pages layer.** `CNAME` contains `nevamis.ca` only. In Porkbun, ensure `www.nevamis.ca` has a CNAME to the Pages host (GitHub then 301s it to the apex), and in the repo's Pages settings confirm "Enforce HTTPS" is ticked. Then test all four combinations (`http://`, `https://`, with and without `www`) resolve to one `https://nevamis.ca/` with a 301, not a 200. Four-URL duplication is the classic silent killer on custom-domain Pages sites.
   impact 3/5 · effort 1/5 · touches: Porkbun DNS, GitHub Pages settings, CNAME

43. **SEO-TECHNICAL-ONPAGE-043 — Make gen-sitemap.mjs refuse to list any page that is not `index, follow`.** The script has a hard-coded `PAGES` array; if `home.html` were ever promoted with the wrong robots value, or a page were set to `noindex` for a campaign, the sitemap would keep advertising it. Add a check that reads each file and throws if it contains `noindex`, and a second check that the file exists. Also assert the file count matches the array length so a new page cannot be forgotten silently.
   impact 3/5 · effort 1/5 · touches: scripts/gen-sitemap.mjs

44. **SEO-TECHNICAL-ONPAGE-044 — Wire sitemap regeneration and the consistency check into the promote step.** `scripts/promote.mjs` currently only flips the robots meta and injects the Google verification tag. Have it also invoke `check-consistency.js` and `gen-sitemap.mjs` and abort the promotion if either fails, so the live homepage can never ship with a stale `lastmod` or a banned phrase. The whole point of the home.html → index.html staging pattern is a gate; right now the gate only checks one thing.
   impact 3/5 · effort 2/5 · touches: scripts/promote.mjs

45. **SEO-TECHNICAL-ONPAGE-045 — Publish an IndexNow key and ping Bing from the promote script.** Drop a `<32-char-key>.txt` file at the repo root containing the key, then have `promote.mjs` POST the changed URLs to `https://api.indexnow.org/indexnow`. Bing indexes new static pages far slower than Google without it, and Bing's index is what backs ChatGPT Search and Copilot — which matters disproportionately for a business selling AI to people who will Google "AI receptionist" and increasingly ask an assistant instead.
   impact 3/5 · effort 2/5 · touches: scripts/promote.mjs, new key file at root, _config.yml (must NOT be excluded)

46. **SEO-TECHNICAL-ONPAGE-046 — Verify app.nevamis.ca as its own Search Console property and confirm the blanket Disallow is holding.** The engine ships `public/robots.txt` with `User-agent: * / Disallow: /` and the pages also carry `noindex` — correct, but unverified. Add the subdomain as a property and check "Pages → Not indexed" is where every URL sits, and that nothing from the client portal or ops console has leaked into the index. A signup or verify URL appearing in Google would be both an SEO and a trust problem.
   impact 3/5 · effort 1/5 · touches: Google Search Console, nevamis-engine/public/robots.txt

47. **SEO-TECHNICAL-ONPAGE-047 — Expand llms.txt into a full answer surface, and reference it from robots.txt.** The current 32-line file is honest and useful (it even tells summarisers not to attribute client counts) but stops at page links. Add the fourteen FAQ answers verbatim, the pilot's exact terms (no card, hard caps, ends day eight), the coverage modes, and the setup timeline. When an owner asks Claude or ChatGPT "does the Nevamis pilot auto-bill me", the answer should come from the file the business controls, not from a hallucination.
   impact 3/5 · effort 2/5 · touches: llms.txt, robots.txt

48. **SEO-TECHNICAL-ONPAGE-048 — Measure and fix the LCP cost of the full-screen `#wake` overlay.** `home.html` paints `#wake{position:fixed;inset:0;z-index:200;background:var(--navy-0)}` over the entire viewport and only fades it when the GSAP hero timeline boots (with a 2500 ms `setTimeout` safety net). Until that fade starts, nothing meaningful is painted, so Chrome's LCP is gated behind `gsap.min.js` + `MotionPathPlugin.min.js` + `assets/motion/main.js` + `hero.js` downloading, parsing and running. Run `npx lighthouse https://nevamis.ca --preset=desktop` and a mobile run, get the real number, then either start the wake fade in the inline head script on `DOMContentLoaded` or make the overlay semi-transparent so the h1 counts as painted.
   impact 5/5 · effort 3/5 · touches: home.html, assets/motion/hero.js

49. **SEO-TECHNICAL-ONPAGE-049 — Preload the two font files the hero actually needs.** `assets/fonts/fonts.css` declares fifteen woff2 faces with `font-display: swap` and nothing is preloaded, so the browser must parse `site.css` → `fonts.css` → discover the font → fetch it, three round trips before the h1 renders in Bricolage Grotesque. Add `<link rel="preload" as="font" type="font/woff2" crossorigin href="assets/fonts/bricolage-grotesque-700-latin.woff2">` and the same for `atkinson-hyperlegible-400-latin.woff2` in every page head. This is the single cheapest LCP win available.
   impact 4/5 · effort 1/5 · touches: all .html heads

50. **SEO-TECHNICAL-ONPAGE-050 — Collapse fonts.css into site.css to remove a render-blocking request chain.** Every page loads `assets/fonts/fonts.css` and then `assets/motion/site.css` as two separate blocking stylesheets, and `fonts.css` is nothing but `@font-face` blocks. Concatenate them (fonts first) into `site.css` and delete the second `<link>`. One fewer blocking round trip on a connection GitHub Pages does not let you tune with headers.
   impact 3/5 · effort 1/5 · touches: assets/motion/site.css, assets/fonts/fonts.css, all .html

51. **SEO-TECHNICAL-ONPAGE-051 — Add `defer` to the two vendor GSAP scripts.** `home.html` lines 817–818 load `gsap.min.js` (72 KB) and `MotionPathPlugin.min.js` (22 KB) as classic blocking scripts, while `site.js`, `motion.js` and `main.js` are all deferred or modules. Because the vendor tags sit at the end of `<body>` this blocks less than it would in the head, but it still stalls the parser before `DOMContentLoaded` — which is what the pricing-preview renderer and the wake-overlay safety net both wait on. `defer` preserves execution order relative to each other and to the deferred scripts that consume them.
   impact 3/5 · effort 1/5 · touches: home.html, and every page that loads GSAP

52. **SEO-TECHNICAL-ONPAGE-052 — Gate the WebGL aurora on viewport and device capability, then measure INP.** `assets/motion/aurora.js` runs a domain-warped fractal-noise fragment shader every frame with scroll-velocity feedback. On a mid-range Android — which is what a contractor holds in a van — that is sustained GPU and main-thread work competing with every tap. Add a guard alongside the existing `prefersReduced` check: skip WebGL init below 900 px width or when `navigator.hardwareConcurrency <= 4`, falling back to the 2D painter that already exists. Then confirm INP with a throttled Lighthouse mobile run before and after.
   impact 4/5 · effort 2/5 · touches: assets/motion/aurora.js, assets/motion/tokens.js

53. **SEO-TECHNICAL-ONPAGE-053 — Reserve layout space for the hero stage and the industry card art to protect CLS.** `.stage` is `height:min(82vh,660px)` on desktop and `clamp(200px,34vh,300px)` on mobile so the SVG is fine, but the four `.ind-card .art` divs load `assets/industries/*.webp` as CSS background images with no `aspect-ratio` declared — if `site.css` sets only a min-height, the cards reflow as images decode. Add `aspect-ratio` to `.art` and audit with Chrome DevTools' Layout Shift Regions overlay on a throttled reload.
   impact 3/5 · effort 2/5 · touches: assets/motion/site.css

54. **SEO-TECHNICAL-ONPAGE-054 — Apply `content-visibility: auto` to the below-fold homepage sections.** `home.html` is 858 lines with fourteen full sections after the hero (`#proof`, `#simulator`, `#how`, `#solutions`, `#industries`, `#roi`, `#process`, `#compare`, `#build-stack`, `#first-week`, `#pricing-preview`, `#risk`, `#beyond`, `#faq`). Add `main > section:not(.hero){content-visibility:auto;contain-intrinsic-size:0 800px}` to `site.css` and verify the scroll-reveal animations and the `#sim` FSM still trigger correctly. This cuts initial style and layout cost substantially on the longest page on the site.
   impact 3/5 · effort 2/5 · touches: assets/motion/site.css, tests/motion.spec.js

55. **SEO-TECHNICAL-ONPAGE-055 — Add a Core Web Vitals budget assertion to the Playwright suite.** The repo has `playwright.config.js` and three spec files but no performance gate, so any future motion addition silently degrades LCP. Add a spec that navigates with CPU throttling, reads `PerformanceObserver` entries for `largest-contentful-paint` and `layout-shift`, and fails above agreed thresholds (LCP 2.5 s, CLS 0.1). Lab numbers are not field numbers, but with zero traffic there is no field data yet — lab is the only signal available.
   impact 3/5 · effort 3/5 · touches: tests/perf.spec.js (new), playwright.config.js

56. **SEO-TECHNICAL-ONPAGE-056 — Preconnect to app.nevamis.ca, which every page hits on load.** `site.js` fires `nvSend("page_view")` to `https://app.nevamis.ca/api/events` immediately, and the header/footer both link to `app.nevamis.ca/login`. Add `<link rel="preconnect" href="https://app.nevamis.ca" crossorigin>` to the head so the DNS lookup, TCP handshake and TLS negotiation for that beacon happen in parallel with the CSS fetch rather than after script execution.
   impact 2/5 · effort 1/5 · touches: all .html heads

57. **SEO-TECHNICAL-ONPAGE-057 — Version the static asset URLs, since GitHub Pages will not let you set cache headers.** `site.css`, `site.js`, `motion.js` and the vendor bundles are served with Pages' default short max-age and no control over it. Append a content-hash or date query string (`site.css?v=20260727`) updated by `promote.mjs`, so you can safely rely on longer client caching where it does occur and repeat visitors never get a stale mixed set of CSS and JS. Small win, but it is the only cache lever this hosting model offers.
   impact 2/5 · effort 2/5 · touches: scripts/promote.mjs, all .html

58. **SEO-TECHNICAL-ONPAGE-058 — Convert the four industry images from CSS backgrounds to real `<img>` elements.** `assets/industries/{electrical,hvac,restoration,automotive}.webp` (112 KB total) are painted via inline `style="background-image:url(...)"` on `aria-hidden` divs, so they are invisible to Google Images, carry no alt text, and cannot be lazy-loaded natively. Replace with `<img src="…" alt="Electrician taking a service call" width="…" height="…" loading="lazy" decoding="async">` inside the card. Trades photography is a real long-tail image-search entry point and costs nothing to unlock.
   impact 3/5 · effort 2/5 · touches: home.html, assets/motion/site.css

59. **SEO-TECHNICAL-ONPAGE-059 — Rename image files to describe what they show.** `electrical.webp`, `hvac.webp`, `restoration.webp`, `automotive.webp` and `daren.jpg` say nothing. Rename to `edmonton-electrician-service-call.webp`, `hvac-furnace-emergency-call.webp`, `restoration-water-damage-call.webp`, `auto-shop-booking-call.webp` and `daren-denchuk-founder-nevamis-ai.jpg`, updating the four inline `background-image` references and `home.html` line 706. Filenames are one of the few remaining image-ranking signals and this is a five-minute change while nothing links to them yet.
   impact 2/5 · effort 1/5 · touches: assets/industries/, assets/daren.jpg, home.html

60. **SEO-TECHNICAL-ONPAGE-060 — Serve the founder photo as WebP at the right intrinsic size.** `assets/daren.jpg` is 30,976 bytes and rendered at `width="104" height="104"`; unless the source is exactly 208×208 it is being downscaled by the browser at cost. Export a 208×208 WebP (should land under 6 KB) and a 104×104 fallback, wire them up with `<picture>` or `srcset="… 1x, … 2x"`, and keep the existing alt text. Roughly 25 KB saved on a page that also carries 630 KB of MP3s.
   impact 2/5 · effort 1/5 · touches: assets/, home.html, about.html

61. **SEO-TECHNICAL-ONPAGE-061 — Add explicit `width`/`height` to the Organization logo in schema and verify the logo asset meets Google's requirements.** The Organization block sets `"logo":"https://nevamis.ca/icon-512.png"` as a bare string. Google wants an `ImageObject` with `url`, `width` and `height`, and the image must be crawlable and at least 112×112 — `icon-512.png` qualifies at 512×512 but the format is not declared. Upgrade to `"logo":{"@type":"ImageObject","url":"…/icon-512.png","width":512,"height":512}`.
   impact 2/5 · effort 1/5 · touches: home.html

62. **SEO-TECHNICAL-ONPAGE-062 — Set a page-weight budget and enforce it, starting with the homepage.** Today the homepage ships roughly 94 KB of vendor JS, 30 KB of CSS, 15 woff2 faces, four WebPs and eleven MP3s totalling ~630 KB (the audio is lazily constructed via `new Audio()` in `site.js`, so it does not load on first paint — verify that stays true). Write the current totals into `README.md` as a budget, and add a check to `check-consistency.js` that sums the byte size of everything referenced from `index.html`'s head and body and fails above the threshold. Budgets only work when a script enforces them.
   impact 2/5 · effort 2/5 · touches: scripts/check-consistency.js, README.md

63. **SEO-TECHNICAL-ONPAGE-063 — Render the homepage plan cards in static HTML instead of building them in JavaScript.** `home.html`'s `#pricePreview` is empty in the source; the three cards are constructed at `DOMContentLoaded` by the inline script at line 822 from `window.NV_PRICING`. `pricing.html` gets this right — it ships a `#plansFallback` with real HTML that the renderer replaces. Do the same on the homepage (or better: ship the static cards and have the script update the numbers in place rather than append), so non-rendering crawlers and every LLM scraper see C$249 / C$449 / C$849 rather than an empty div. Pricing is the number one thing an AI answer engine gets asked to fetch.
   impact 4/5 · effort 2/5 · touches: home.html, pricing-config.js

64. **SEO-TECHNICAL-ONPAGE-064 — Verify with Search Console's URL Inspection that Googlebot actually sees the rendered pricing on pricing.html.** The static `#plansFallback` block is removed from the DOM "only after a successful render", which means the rendered HTML Googlebot indexes depends on `pricing-config.js` executing. Use Inspect URL → Test Live URL → View Crawled Page → Screenshot and HTML on `https://nevamis.ca/pricing.html`, and confirm the three plans and the recommender are present. If they are not, keep the fallback permanently in the DOM and have the renderer mutate it rather than delete it.
   impact 4/5 · effort 1/5 · touches: Google Search Console, pricing.html

65. **SEO-TECHNICAL-ONPAGE-065 — Work the Search Console setup list this week, in this order.** (a) Add `nevamis.ca` as a *Domain* property, not just the URL-prefix property the existing `google-site-verification` meta covers, so www/http/apex variants roll up together. (b) Submit `sitemap.xml` and confirm "10 discovered". (c) Request indexing individually for `/`, `/pricing.html`, `/demo.html` and `/pilot.html`. (d) Check Pages → Why pages aren't indexed for the `brand/` and `concept*.html` URLs and confirm they disappear after the `_config.yml` exclusions. (e) Import the property into Bing Webmaster Tools (one-click from GSC). (f) Set a monthly recurring reminder to export the Performance query list — with zero clients and near-zero traffic, the first twenty impressions Google records are the only unbiased demand signal the business has about what Edmonton owners actually type.
   impact 4/5 · effort 2/5 · touches: Google Search Console, Bing Webmaster Tools, _config.yml
