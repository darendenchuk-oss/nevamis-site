# Retention, Expansion and Referral — 55 improvements

Nevamis has zero paying clients today, which makes this domain look premature — it is the opposite. The first five founding clients are the entire proof supply for every sales asset the business will ever need, and the systems that turn them into renewals, upgrades, referrals and reviews have to exist *before* they arrive, not after the first one quietly stops forwarding their line. The engine already has most of the raw material: `onboardingProjects`/`onboardingTasks` with a `hypercare` stage that has no tasks in it, `tenants.status` with a `paused` value nothing ever writes, `billingCredits` with no referral reason, `changeRequests`, `qualityReviews`, `usageAlerts`, `portalMetrics`, and a live `/ops/day8` packet that computes real numbers with no fabrication. The site has a risk-reversal section, a founding-client offer, and a 14-item FAQ that ends at "what if it is not a fit" — and no page at all about what month two looks like. `ai-assistant/outreach/REFERRAL-OFFER.md` is written but still marked DRAFT, and `docs/OFFBOARDING-SOP.md` is genuinely good but disconnected from any data. The ideas below turn those loose parts into a retention program: define first value and instrument it, prove captured revenue with numbers the client themselves confirmed, put a rhythm on the tuning reviews that are already being sold, name the churn signals with thresholds, build the upgrade ladder, ship the referral terms, and collect case studies and reviews with permission and without ever gating a waiver on a positive word.

---

1. **RETENTION-EXPANSION-REFERRAL-001 — Define "first value" as one timestamped event and instrument it.** Add `firstValueAt` and `firstValueCallId` to `tenants` in `src/db/schema.ts`, written the first time a `callSummaries` row for that tenant has `bookingConfirmed = true` from a caller who is not Daren. Every other metric in this domain hangs off that moment: time-to-first-value, the recording you send them, the day-30 review anchor. Until it is a column, "did it work for them yet" is a feeling rather than a number, and you cannot tell a stalled client from a slow week.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts, src/domain/call-intake.ts, drizzle/

2. **RETENTION-EXPANSION-REFERRAL-002 — Send the client the recording of the first real call it booked, within 24 hours, with a two-line email.** Add a `firstBookedCall` template to `src/lib/mailer.ts`: subject `Your AI just booked its first job`, body naming the caller's first name, the service, the appointment time, and a link to the call in the portal. This is the single highest-emotion moment of the whole relationship and right now nothing marks it. Fire it from the same code path that sets `firstValueAt` so it can never be sent twice.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/lib/mailer.ts, src/domain/call-intake.ts

3. **RETENTION-EXPANSION-REFERRAL-003 — Give the `hypercare` stage real tasks instead of leaving it an empty label.** `taskPlanFor()` in `src/domain/onboarding.ts` stops at the go-live readiness gate, yet `STAGES` in `src/app/portal/page.tsx` lists `hypercare` and `stabilized` after `launch`. Append five nevamis-side tasks that fire post-launch: day 1 forwarding verification call, day 3 first call-log review, day 7 tuning pass, day 14 owner check-in, day 30 review booked. A client who sees five things Nevamis owes them in week one does not feel abandoned after the invoice clears.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/onboarding.ts, src/app/portal/page.tsx

4. **RETENTION-EXPANSION-REFERRAL-004 — Capture the owner's own success sentence at kickoff and quote it back at every review.** Add `successCriterion` (free text) and `successCriterionAskedAt` to `tenantProfiles`, filled on the discovery call by literally asking "in 30 days, what has to have happened for this to be obviously worth $449 a month?". Render it at the top of `/portal/performance` and at the top of the day-30 review agenda. Retention arguments made in the client's own words beat any dashboard you can build.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts, src/app/portal/performance/page.tsx, docs/pilot/pilot-intake-form.md

5. **RETENTION-EXPANSION-REFERRAL-005 — Detect the silent-forwarding failure in the first 14 days, not the first invoice dispute.** The most common quiet death for a forwarded-line product is the owner turning forwarding off, a carrier resetting it, or the office manager undoing it — and nobody noticing until the client says "it never rang". Write `src/domain/health-signals.ts` with `forwardingSilence(tenantId)`: if a tenant is `active`, launched more than 48 hours ago, and has zero `calls` rows in the last 48 hours, raise it. Surface it as an action-queue item titled "Line silent 48h — call them and test the number".
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/health-signals.ts (new), src/app/ops/queue/page.tsx

6. **RETENTION-EXPANSION-REFERRAL-006 — Ship a one-page "tell your team this" handout the client can print and pin in the shop.** Write `docs/client-kit/TEAM-BRIEFING.md` in the engine: what the AI does, what it will never do (quote prices you have not approved, dispatch anyone), how a call gets transferred to a human, and who to text when it gets something wrong. Office staff and lead techs kill these systems more often than owners do, usually because nobody told them what it was. Link it from `/portal/help` as "Give this to your crew".
impact 4/5 · effort 1/5 · touches: nevamis-engine/docs/client-kit/TEAM-BRIEFING.md (new), src/app/portal/help/page.tsx

7. **RETENTION-EXPANSION-REFERRAL-007 — Produce a signed build sheet at go-live: what we agreed the agent will and will not do.** Generate a printable page at `/ops/accounts/[id]/build-sheet` from `agentManifests` and `tenantProfiles`: hours covered, coverage mode, services it can book, questions it asks, escalation rules, what it refuses. Have the client approve it via the existing `clientApprovals` mechanism. Almost all "it did the wrong thing" churn is an expectation gap, and a countersigned scope page converts that fight into a `changeRequests` row.
impact 4/5 · effort 3/5 · touches: nevamis-engine/src/app/ops/accounts/, src/db/schema.ts (clientApprovals), src/domain/manifest.ts

8. **RETENTION-EXPANSION-REFERRAL-008 — Store the owner's average job value once, with the date they said it, and never estimate without it.** Add `avgJobValueCents` and `avgJobValueStatedAt` to `tenantProfiles`. `docs/pilot/day7-pilot-report-template.md` already insists potential value uses the owner's own inputs and is labelled an estimate — encode that rule so `/portal/performance` and `/ops/day8` render the value line only when the field is present, and otherwise render "tell us your average job value and we will show this". A dated, owner-provided number is honest; a hardcoded $450 is a lie waiting to be caught.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts, src/domain/portal-metrics.ts, src/app/portal/performance/page.tsx

9. **RETENTION-EXPANSION-REFERRAL-009 — Let the owner mark a booked call as a real job with a real dollar figure, turning estimates into confirmed facts.** Add a two-field form on each call in the portal: "did this become a job?" and "what did it bill?" writing to `jobs` and `payments` for that tenant. Then `portalMetrics.totals.collectedCents` stops being synthetic and becomes owner-confirmed. Three owner-confirmed jobs at $1,200 each is the strongest retention artifact the business can ever own — and the only one no competitor can fake either.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/app/portal/, src/domain/portal-metrics.ts, src/db/schema.ts (jobs, payments)

10. **RETENTION-EXPANSION-REFERRAL-010 — Compute the "would have been missed" count, because that is the number they are actually buying.** `/ops/day8` already has `isAfterHours()` doing an Edmonton-clock split. Promote it to `src/domain/coverage.ts`, teach it the tenant's own `businessHours` from `tenantProfiles` instead of a hardcoded 8–17, and render "N of your M calls this month came in when nobody was there to answer" on `/portal/performance`. That single sentence is the entire product value proposition stated as a fact about their own business.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/coverage.ts (new), src/app/ops/day8/page.tsx, src/app/portal/performance/page.tsx

11. **RETENTION-EXPANSION-REFERRAL-011 — Ship a monthly value statement at `/portal/report/[month]`, print-styled, one page.** Calls answered, after-hours share, leads captured, bookings confirmed, owner-confirmed jobs and value, minutes used against included minutes, and what the month cost. It must render "0" honestly with a plain sentence rather than hiding the section. Email a link on the 1st via a `monthlyReport` mailer template — a report they never open still tells them somebody is watching their line.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/app/portal/report/[month]/page.tsx (new), src/lib/mailer.ts, src/domain/portal-metrics.ts

12. **RETENTION-EXPANSION-REFERRAL-012 — Put a cost-per-captured-lead line on the monthly report.** Divide the month's plan fee plus overage by leads captured: "$449 ÷ 31 leads = $14.48 per lead this month". Trades owners already know what a Google Ads lead costs them and the comparison lands without any claim from you. Guard the divide-by-zero with "no leads this month — let us look at your call flow", which is itself the right message.
impact 4/5 · effort 1/5 · touches: nevamis-engine/src/app/portal/report/[month]/page.tsx, src/domain/money.ts

13. **RETENTION-EXPANSION-REFERRAL-013 — Build a monthly three-call highlight reel with an explicit recording-permission gate.** Pick the longest booked call, the best after-hours catch, and one emergency correctly escalated, and put three play buttons in the monthly report. Only render them when the tenant's intake recorded consent to recording (`docs/pilot/pilot-intake-form.md` already asks). Owners forget what the thing does between invoices; ninety seconds of their own customer being handled well fixes that.
impact 4/5 · effort 3/5 · touches: nevamis-engine/src/app/portal/report/, src/db/schema.ts (calls.recordingUrl), src/domain/calls.ts

14. **RETENTION-EXPANSION-REFERRAL-014 — Send a Friday digest that is four numbers and one call, and nothing else.** `/ops/weekly` already exists on the operator side; add a client-facing `weeklyDigest` template in `src/lib/mailer.ts` that sends calls answered, after-hours caught, bookings, and one line about the most interesting call. Cap it at what fits in an SMS preview. Weekly presence is what makes a monthly invoice feel like a subscription rather than a charge.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/lib/mailer.ts, src/app/ops/weekly/page.tsx, src/lib/weekly.ts

15. **RETENTION-EXPANSION-REFERRAL-015 — Schedule the tuning reviews you are already selling.** `pricing-config.js` promises "One monthly tuning review" on After Hours and "Two tuning reviews per month for the first 90 days" on Growth. Nothing in the engine creates, tracks or proves those. Generate recurring `onboardingTasks` (or a small `serviceCommitments` table) per plan, visible in the client portal as "your next tuning review: Aug 14", and count completed reviews on the monthly report. Selling a deliverable you never schedule is the fastest way to lose a Growth client at month three.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/domain/onboarding.ts, nevamis-site/pricing-config.js, src/app/portal/page.tsx

16. **RETENTION-EXPANSION-REFERRAL-016 — Write the day-30 review agenda as a real document, modelled on the day-7 pilot report.** Create `docs/reviews/DAY30-REVIEW.md`: their numbers first, the success criterion from idea 004 read back verbatim, what we changed during hypercare, what we want to change next, the honest question "is this earning its keep", and the referral moment only if the answer was yes. `docs/pilot/day7-pilot-report-template.md` proves the format works; month one deserves the same rigour.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/reviews/DAY30-REVIEW.md (new), ai-assistant/outreach/

17. **RETENTION-EXPANSION-REFERRAL-017 — Add day-60 and day-90 agendas with different jobs, not the same call three times.** Day 60 is about expansion signals: are they hitting minutes, is a second flow obvious, has volume changed. Day 90 is the retention decision point: annual prepay offer, case-study ask, referral ask, and the frank "what would make you cancel" question. Put all three plus day 30 into a `docs/reviews/` folder and add them to `ai-assistant/WEEKLY-RHYTHM.md` so they appear in Daren's week rather than living as good intentions.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/reviews/, ai-assistant/WEEKLY-RHYTHM.md

18. **RETENTION-EXPANSION-REFERRAL-018 — Build a client health score on `/ops/accounts` from signals that already exist.** Five inputs, each red/amber/green: calls in the last 7 days versus their own baseline, open `qualityReviews` older than 3 days, `changeRequests` in `requested` state older than 5 days, days since last portal login, and minutes used against plan. Sort the accounts list worst-first. With five clients you can hold this in your head; the point is that the system holds it when you are on a roof measuring for the sixth.
impact 4/5 · effort 3/5 · touches: nevamis-engine/src/app/ops/accounts/page.tsx, src/domain/health-signals.ts

19. **RETENTION-EXPANSION-REFERRAL-019 — Write the churn-signal register with actual thresholds, and treat missing a threshold as a system bug.** `docs/CHURN-SIGNALS.md`: silence 48h, minutes down more than 50% month over month, no portal login in 30 days, a change request open more than 7 days, two high-severity quality flags in 14 days, an unanswered day-30 review invite, and any mention of "we're slow right now" in a call. Each row names the signal, the threshold, the action, and who does it. `docs/SUPPORT-SLA.md` already proves this business writes good threshold tables — copy the discipline over to retention.
impact 4/5 · effort 1/5 · touches: nevamis-engine/docs/CHURN-SIGNALS.md (new), docs/SUPPORT-SLA.md

20. **RETENTION-EXPANSION-REFERRAL-020 — Detect usage collapse, which is churn in slow motion.** Add `usageTrend(tenantId)` to `src/domain/usage.ts` comparing connected minutes this calendar month against last. A drop over 50% for a client who has not told you about a seasonal slowdown almost always means forwarding is off, the number changed, or a receptionist got hired. Raise it into the action queue with the two months' figures in the title so the call writes itself.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/domain/usage.ts, src/app/ops/queue/page.tsx

21. **RETENTION-EXPANSION-REFERRAL-021 — Make an aged change request a churn alarm rather than a backlog item.** `changeRequests` already models `requested → approved → in_build → tested → released`. A trades owner who asked for a wording fix twelve days ago and heard nothing has already decided you are not serious. Add an age badge and an ops alert at 5 days in `requested`, and close the loop by emailing the client when the status hits `released` — "you asked, it is live, here is the wording".
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts (changeRequests), src/app/ops/queue/page.tsx, src/lib/mailer.ts

22. **RETENTION-EXPANSION-REFERRAL-022 — Make `tenants.status = "paused"` a real, sellable state instead of an unused enum value.** Edmonton trades are violently seasonal: a landscaper in January and a furnace shop in July both have a month where $449 feels absurd. Ship a pause flow — service off, number parked (the offboarding SOP already prices parking at about $1.15/mo), portal read-only, billing stopped, a stated resume date, no setup fee to come back. A pause you offer is a cancellation you did not receive.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/domain/billing.ts, src/app/ops/accounts/, docs/OFFBOARDING-SOP.md

23. **RETENTION-EXPANSION-REFERRAL-023 — Add a documented downgrade ladder so "too expensive" never has to mean "cancel".** Growth → After Hours → Pay As You Go ($49/mo + $1.95/min) is a real path already priced in `pricing-config.js`, and no setup fee applies to a move between existing plans. Write the ladder into `docs/CHURN-SIGNALS.md` with the exact sentence to say, and make the plan change a one-click action on `/ops/billing`. A client on PAYG is still a client, still referring, and still there when their season turns.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/app/ops/billing/page.tsx, src/domain/billing.ts, nevamis-site/pricing-config.js

24. **RETENTION-EXPANSION-REFERRAL-024 — Capture cancellation reasons as structured codes, not as a sentence in an email you will never re-read.** `docs/OFFBOARDING-SOP.md` step 1 already asks the perfect question — "what would have made this worth keeping?" — and then puts the answer in "the tracker row". Add a `churnReasons` table (tenantId, code, verbatim, wouldReturn, askedAt) with a fixed code list: price, low call volume, hired a receptionist, quality, business closed or sold, seasonal, integration missing, other. With five clients this is small; with fifteen it is the roadmap.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts, src/app/ops/accounts/, docs/OFFBOARDING-SOP.md

25. **RETENTION-EXPANSION-REFERRAL-025 — Handle involuntary churn before it exists: a dunning sequence on `past_due`.** `subscriptions.status` already has `past_due` and `src/domain/stripe-billing.ts` plus the Stripe webhook route are wired. A failed card should never silently kill service for a client who wants the product: day 0 email, day 3 email plus SMS, day 7 personal call, day 10 pause rather than terminate. Write it in `docs/DUNNING.md` and implement the state transitions in `src/domain/billing.ts`.
impact 4/5 · effort 3/5 · touches: nevamis-engine/src/domain/billing.ts, src/app/api/webhooks/stripe/route.ts, docs/DUNNING.md (new)

26. **RETENTION-EXPANSION-REFERRAL-026 — Write the save-conversation script, with the offers you are actually willing to make written down in advance.** `ai-assistant/outreach/` has objection cards for sales and nothing for saves. Create `SAVE-CONVERSATION.md` covering the five churn reasons from idea 024, each with one diagnostic question and one concrete lever (pause, downgrade, a specific tuning fix, a scoped change request, or a clean honest exit). Decide the limits at a desk, not on a call where you will over-discount out of panic.
impact 4/5 · effort 1/5 · touches: ai-assistant/outreach/SAVE-CONVERSATION.md (new)

27. **RETENTION-EXPANSION-REFERRAL-027 — Schedule a 60-day win-back that only fires when something genuinely changed.** Add a `winBackEligibleAt` timestamp set at offboarding and a hard rule in `docs/CHURN-SIGNALS.md`: contact only if the reason they gave has been demonstrably addressed — the integration now exists, the quality issue is fixed, the season turned. "Just checking in" to a churned trades owner burns the relationship and the referral. A specific "you left because we could not book into Jobber; we can now" is a real offer.
impact 3/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts, docs/CHURN-SIGNALS.md

28. **RETENTION-EXPANSION-REFERRAL-028 — Publish the "how to leave" section on the site, because saying it out loud is what closes the sale.** Add a block to `pilot.html` and an FAQ entry in `home.html` (`#faq`) that states plainly: email to cancel, service runs to the end of the paid period, you turn forwarding off with a code from your own phone, your number was never ours, we export your call log on request, and unused full months of an annual prepay are refunded — all of which is already true in `docs/OFFBOARDING-SOP.md` and the pricing config. Reversibility sells; it also sets expectations that make the eventual exit non-hostile.
impact 4/5 · effort 1/5 · touches: nevamis-site/home.html (#faq), pilot.html, terms.html

29. **RETENTION-EXPANSION-REFERRAL-029 — Add a "month two onward" section to the homepage, because the site currently ends at go-live.** `#first-week` in `home.html` walks day 1 to day 7 and then the narrative stops. Add a short companion block: a tuning review every month, a call-log review, a monthly numbers report, a named person who answers, and change requests as part of the service. Prospects on a $449/mo month-to-month plan are silently asking "what am I paying for in month six" and nothing on the page answers.
impact 5/5 · effort 2/5 · touches: nevamis-site/home.html (#first-week), assets/motion/site.css

30. **RETENTION-EXPANSION-REFERRAL-030 — Turn a hit minute cap into an upgrade decision with the arithmetic shown.** `src/components/usage-meter.tsx` already alerts at 50/75/90/100%. At 90% on After Hours, render the comparison in dollars: 250 included plus 120 overage minutes at $1.10 is $381 on top of $249; Growth is $449 flat with 600 minutes. Add a "move me to Growth" button that files a change request. Upgrades should be the client's obvious arithmetic, never your ask.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/components/usage-meter.tsx, src/domain/usage.ts, nevamis-site/pricing-config.js

31. **RETENTION-EXPANSION-REFERRAL-031 — Make Pay As You Go compute its own break-even and say it.** At $49/mo plus $1.95 per connected minute, a PAYG client passes the $249 After Hours price at about 103 minutes, and After Hours includes 250. Render "you used 141 minutes last month ($324) — After Hours would have been $249 with 109 minutes to spare" on the PAYG usage view. PAYG exists to lower the entry barrier; it should graduate people automatically rather than quietly overcharging them.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/domain/usage.ts, src/components/usage-meter.tsx

32. **RETENTION-EXPANSION-REFERRAL-032 — Offer annual prepay at day 90, not at signup.** The 10-months-for-12 deal in `pricing-config.js` is the best retention instrument the business has, and it is worthless offered to a stranger and powerful offered to a client who just saw three months of their own numbers. Add it as a scripted step in the day-90 review agenda with the exact figures ($4,490 for a year of Growth, two months free, unused full months refunded on cancellation) and a note that the refund promise is what makes the ask fair.
impact 5/5 · effort 1/5 · touches: nevamis-engine/docs/reviews/DAY90-REVIEW.md, nevamis-site/pricing-config.js

33. **RETENTION-EXPANSION-REFERRAL-033 — Write the coverage-mode expansion ladder with a trigger for each rung.** After hours only → after hours plus missed-call overflow → full-time front line. Each rung has an observable trigger: overflow when the daytime missed-call count from their own carrier or `calls` data is non-trivial, full-time when they mention hiring or when a person is tied to the phone. `home.html` `#solutions` already shows coverage modes as choices; internally they should be a sequence with evidence attached.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/reviews/, nevamis-site/home.html (#solutions)

34. **RETENTION-EXPANSION-REFERRAL-034 — Publish a small add-on price list so expansion does not require a whole plan change.** Second call flow, second calendar, an extra department line, a second location, an additional language, an extra tuning review cadence. Put the items in `pricing-config.js` as an `addOns` array with "quoted on the call" where a fixed price is not yet honest, and render them on `pricing.html` under the plan table. Without this, every small expansion turns into a renegotiation and most simply never happen.
impact 4/5 · effort 2/5 · touches: nevamis-site/pricing-config.js, pricing.html

35. **RETENTION-EXPANSION-REFERRAL-035 — Detect the second-location and second-number signal from call content.** `callSummaries` captures `service` and the transcript already exists; a client whose callers keep asking about a service or an area outside the configured rules is a client whose agent is under-scoped. Add a monthly "questions your AI could not answer" list on the client report, drawn from calls where `humanFollowUpRequired` is true. It is simultaneously a quality report, an upgrade pitch, and proof you are reading their calls.
impact 4/5 · effort 3/5 · touches: nevamis-engine/src/domain/calls.ts, src/app/portal/report/

36. **RETENTION-EXPANSION-REFERRAL-036 — Track net revenue retention in `/ops/revenue` once a real subscription exists.** Expansion minus contraction minus churn on the existing client base, monthly, from `subscriptions` and `billingInvoices` — and rendered as "not enough history yet" until there is. With five clients the number is noise, but building it now means the first honest cohort chart appears automatically rather than being reconstructed a year late from Stripe exports.
impact 3/5 · effort 3/5 · touches: nevamis-engine/src/app/ops/revenue/page.tsx, src/domain/billing.ts

37. **RETENTION-EXPANSION-REFERRAL-037 — Put a Revenue Engine interest register inside the client portal, honestly labelled.** `roadmap-config.js` lists planned modules and `coming-soon.html` sells them as PLANNED. Add a portal card: "We are building lead follow-up, quote recovery and review requests. Which would you use first?" writing to the existing `interestRequests` table with a source of `portal`. Your paying clients are the only people whose build-order vote is worth anything, and asking them is itself a retention act.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/app/portal/, src/domain/interest.ts, nevamis-site/roadmap-config.js

38. **RETENTION-EXPANSION-REFERRAL-038 — Run a quarterly business-drift check, because a stale agent is a cancelled agent.** Hours change, prices change, a service gets dropped, the service area expands to Sherwood Park, a tech quits and the escalation number is dead. Add a `/portal/business` prompt after 90 days without an edit — "confirm these are still right" — with a one-click "still accurate" that stamps `verifiedAt`. An agent quoting last year's hours does visible damage to the client's reputation, and they will blame you correctly.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/app/portal/business/page.tsx, src/db/schema.ts (tenantProfiles)

39. **RETENTION-EXPANSION-REFERRAL-039 — Run seasonal readiness touches on the Edmonton trades calendar.** Late August for furnace season, early January for frozen pipes and no-heat calls, May for AC and irrigation, and spring melt for restoration. Each is a short email plus a tuning pass offer: "your no-heat script should lead with 'is anyone without heat right now'". Write them once in `docs/reviews/SEASONAL-TOUCHES.md` with send windows. It is a proactive check-in that arrives exactly when the client's call volume is about to spike, which is also when minutes and upgrades sell themselves.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/reviews/SEASONAL-TOUCHES.md (new), src/lib/mailer.ts

40. **RETENTION-EXPANSION-REFERRAL-040 — Bridge every high-severity quality flag to a same-day human call, and log that it happened.** `docs/SUPPORT-SLA.md` promises "human listens same day" on a flagged call. Extend that: when a `qualityReviews` row is created with `severity = "high"`, create a queue task "call the owner about this call today" and record the outcome in `resolutionNote`. Clients forgive a bad AI call; they do not forgive finding out about it themselves and hearing nothing from you.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/escalation.ts, src/app/ops/queue/page.tsx, docs/SUPPORT-SLA.md

41. **RETENTION-EXPANSION-REFERRAL-041 — Publish a monthly "what we changed for you" line on the report.** Pull released `changeRequests` and agent version bumps from `agentDeployedSnapshots` for the month and list them in plain language: "Added your new Leduc service area", "Stopped it offering Saturday slots", "Fixed the pronunciation of your company name". Invisible work is unpaid work; a subscription survives on visible maintenance.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/app/portal/report/, src/db/schema.ts (agentDeployedSnapshots, changeRequests)

42. **RETENTION-EXPANSION-REFERRAL-042 — Get the referral offer out of DRAFT and into a decided, written term.** `ai-assistant/outreach/REFERRAL-OFFER.md` is fully worked out — 50% off one month for both sides, conversion defined as the referred business completing a pilot and starting a paid month, credit applied on the next invoice, no cash, no retroactive claims after 30 days, and a correct CASL note that Nevamis does not cold-email the referred business. It has been sitting at "DRAFT until Daren confirms" while the whole referral surface waits on it. Confirm the terms, remove the DRAFT banner, and date the decision in `docs/DECISIONS.md`.
impact 5/5 · effort 1/5 · touches: ai-assistant/outreach/REFERRAL-OFFER.md, nevamis-engine/docs/DECISIONS.md

43. **RETENTION-EXPANSION-REFERRAL-043 — Implement referral credits in the engine so the promise cannot be forgotten.** Add `referredByTenantId` to `tenants`, a `referral_credit` value to the `billingCredits.reason` enum, and a small `/ops/accounts` action that issues both credits at once when a referred client's first paid month completes. Manual credit tracking survives exactly two referrals; the third gets missed and the referrer never mentions it, they just stop referring.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts, src/domain/billing.ts, src/app/ops/accounts/

44. **RETENTION-EXPANSION-REFERRAL-044 — Name the three moments the referral ask is made, and make them tasks.** After the first owner-confirmed booked job, at the end of the day-30 review when the answer to "is this earning its keep" was yes, and at the close of any successful save. `docs/pilot/pilot-end-teardown-checklist.md` already has a "one referral ask made regardless of outcome" checkbox — extend that discipline past the pilot. An unasked referral is the cheapest lead the business will never get.
impact 5/5 · effort 1/5 · touches: nevamis-engine/docs/reviews/, docs/pilot/pilot-end-teardown-checklist.md

45. **RETENTION-EXPANSION-REFERRAL-045 — Build a `/referral.html` page a client can forward without you writing an email each time.** The offer in one paragraph, the mechanics as five bullets, a "who this is for" line (Edmonton service business, misses calls, owner-operated), and a form that captures the referrer's name so credit is unambiguous. Add it to `sitemap.xml` and `content-map.json`, and link it from the portal and the footer. The referrer's job should be forwarding one URL, not explaining your business.
impact 4/5 · effort 2/5 · touches: nevamis-site/referral.html (new), sitemap.xml, content-map.json, home.html footer

46. **RETENTION-EXPANSION-REFERRAL-046 — Ask for cross-trade referrals specifically, because that is how trades actually refer.** An electrician does not know five other electricians they want to help; they know the plumber, the HVAC guy, the restoration crew and the roofer they are on site with every week. Change the ask from "know another electrician?" to "who do you send work to?" and record the named trade in the prospect tracker. `ai-assistant/outreach/edmonton-trades-starter-list.md` becomes warm rather than cold overnight.
impact 5/5 · effort 1/5 · touches: ai-assistant/outreach/REFERRAL-OFFER.md, outreach/edmonton-trades-starter-list.md, prospect-tracker.csv

47. **RETENTION-EXPANSION-REFERRAL-047 — Make the introduction easy by writing the forwardable message for them.** Add to the referral page and the review-call script a three-sentence text the client can paste: what Nevamis does, that it caught N of their own after-hours calls last month, and Daren's number. `docs/AUTOMATIONS.md` already forbids fabricated statistics, so this template must interpolate the referrer's real number or omit it. People refer when the effort is one paste, not one composition.
impact 4/5 · effort 1/5 · touches: nevamis-site/referral.html, ai-assistant/outreach/DAY8-REVIEW-SCRIPT.md

48. **RETENTION-EXPANSION-REFERRAL-048 — Thank referrers in a way a trades owner notices, on top of the credit.** A handwritten card, or a coffee-shop card dropped at their shop, logged in the account notes. The credit is the fair part of the deal; the physical thank-you is what makes a second referral happen. Keep it small enough that it never reads as buying an introduction, and never contingent on the referred business converting.
impact 3/5 · effort 1/5 · touches: nevamis-engine/docs/reviews/, ai-assistant/outreach/REFERRAL-OFFER.md

49. **RETENTION-EXPANSION-REFERRAL-049 — Write the case-study consent form as a separate document from the founding-client waiver.** `docs/legal/pilot-agreement.md` correctly separates the setup-fee waiver from any review obligation. Case-study permission needs the same care: a one-page form listing exactly what may be published — business name or anonymised descriptor, trade, city, which metrics, whether a call recording may be used, whether their logo may appear — with a right to withdraw and a named contact. Get it signed before you write a word.
impact 5/5 · effort 2/5 · touches: nevamis-engine/docs/legal/case-study-consent.md (new), docs/LEGAL-REVIEW-PACKAGE.md

50. **RETENTION-EXPANSION-REFERRAL-050 — Build the case-study interview kit so the first one is captured at the right moment, not reconstructed later.** Twelve questions in `docs/client-kit/CASE-STUDY-INTERVIEW.md`: what were you doing about missed calls before, what did you expect, what actually happened in week one, what surprised you, what still annoys you, what would you tell another owner. Record it with permission. The strongest quote in the whole business will come out of a client's mouth in month two, and it is gone if nobody was writing it down.
impact 5/5 · effort 1/5 · touches: nevamis-engine/docs/client-kit/CASE-STUDY-INTERVIEW.md (new)

51. **RETENTION-EXPANSION-REFERRAL-051 — Design the case-study page format anonymous-first, so it can ship before anyone agrees to be named.** "An Edmonton electrical contractor, four vans, owner-operated" with real numbers and no name is honest, publishable, and far easier to get permission for than a logo. Build `/work/index.html` plus `/work/<slug>.html` on the existing design system in `assets/motion/site.css`, with a standing rule that every number on the page carries a source and an entry in `docs/CLAIMS-LEDGER.md`. Ship the template now, empty, so the first client only needs to say yes.
impact 5/5 · effort 3/5 · touches: nevamis-site/work/ (new), docs/CLAIMS-LEDGER.md, sitemap.xml

52. **RETENTION-EXPANSION-REFERRAL-052 — Add the review pipeline: the ask, the link, the timing, and the absolute rule that nothing is gated.** Google Business Profile is the review surface Edmonton trades owners themselves live on. Write `docs/client-kit/REVIEW-ASK.md` with the timing (after an owner-confirmed booked job, never in the same breath as an invoice), the exact wording, the direct GBP review URL, and the standing rules already in `docs/OWNER-RUNBOOK.md`: never require a positive review, never condition the setup waiver on one, never offer anything in exchange. `ai-assistant/GBP-DECISION.md` should be checked first for where the profile currently stands.
impact 5/5 · effort 1/5 · touches: nevamis-engine/docs/client-kit/REVIEW-ASK.md (new), ai-assistant/GBP-DECISION.md

53. **RETENTION-EXPANSION-REFERRAL-053 — Write the review-response SOP before the first review, including the bad one.** Every review gets a reply within two business days: thank, name the specific thing, no marketing copy. A negative one gets a factual reply, an offer to fix it offline, and a `qualityReviews` row. A single well-handled one-star reply persuades more sceptical trades owners than five five-star reviews, and the first review will arrive on a day when improvising is a bad idea.
impact 3/5 · effort 1/5 · touches: nevamis-engine/docs/client-kit/REVIEW-ASK.md, docs/CRISIS-COMMS-TEMPLATES.md

54. **RETENTION-EXPANSION-REFERRAL-054 — Put a real proof slot on the homepage that renders nothing until there is something true to put in it.** Between `#risk` and `#beyond` in `home.html`, add a section whose content is driven by a `proof-config.js` array that ships empty, with the section hidden when the array has no entries. When the first consented case study exists it appears everywhere at once without a redesign, and `tests/*.spec.js` can assert the section stays absent while the array is empty — a structural guarantee against fake social proof.
impact 4/5 · effort 2/5 · touches: nevamis-site/home.html, proof-config.js (new), tests/

55. **RETENTION-EXPANSION-REFERRAL-055 — Run a monthly retention review on yourself, five clients or fifty.** Add a section to `ai-assistant/WEEKLY-RHYTHM.md`: on the first business day of each month, walk every active tenant's health score, confirm each has a next scheduled touch on the calendar, check the tuning reviews owed against the tuning reviews delivered, and read the newest churn reason out loud. `docs/MONTHLY-CLOSE.md` already exists for money; this is the same discipline for relationships, and it is the one that stops the second client leaving for a reason the first client already told you about.
impact 4/5 · effort 1/5 · touches: ai-assistant/WEEKLY-RHYTHM.md, nevamis-engine/docs/MONTHLY-CLOSE.md
