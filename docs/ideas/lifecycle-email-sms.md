# Lifecycle Email & SMS — 60 improvements

> **SUPERSEDED 2026-08-09 — the commercial model this file was written against no longer exists.**
> Ideas below were authored while Nevamis sold the C$249 / C$449 / C$849 ladder (plans named
> After Hours, Growth and Scale), a Pay As You Go tier at C$49 + C$1.95/min, annual prepay, a
> setup fee with a founding-client waiver, and a 7-day live pilot — free at first, then C$150.
> Every one of those is retired. The current model is ONE recurring price per plan, charged the
> day the client subscribes and every month after: **Core C$250/month · Growth C$500/month · Pro
> C$1,000/month**, with 250 / 600 / 1,400 included minutes and C$1.10 / C$0.90 / C$0.75 overage,
> nothing charged beside it, and no pilot or trial at any price. `pricing-config.js` is the source
> of truth; docs/CLAIMS-LEDGER.md row CLM-18 is the approval.
>
> The ideas are kept rather than deleted: most are about how a price is *presented*, and that work
> survives the change. But no figure, plan name or offer quoted below may be copied onto a surface,
> and any idea whose whole premise is a setup fee, a pilot, PAYG or annual prepay is moot.


Right now Nevamis has almost no working lifecycle communication, and the reason is unusually concrete: `nevamis-engine/src/lib/mailer.ts` defaults to `NEVAMIS_EMAIL_MODE=off`, `docs/EMAIL-DNS-AUDIT.md` confirms there is no DMARC record and no Resend DKIM on nevamis.ca, and the only four templates that exist are `verification`, `passwordReset`, `workspaceLinked`, and `agentReady`. Everything else in the funnel is a markdown template a human retypes — `docs/pilot/daily-call-log-digest-template.md` is compiled by hand each morning, `day7-pilot-report-template.md` is filled in by hand, and the day-8 promise printed on `pilot.html` ("Silence never becomes a subscription") has no email behind it at all. The CASL surface is thinner than it looks: `coming-soon.html` collects a consent checkbox (`#ifConsent`) that `src/app/api/interest/route.ts` never stores, there is no `/unsubscribe` page anywhere on nevamis.ca, `src/lib/twilio.ts` can physically only text the founder's own cell, and `consentRecords` in `db/schema.ts` requires a `contactId` that prospects do not have. The 60 items below build the sequences in the order the business will actually need them — pilot first, because the pilot is the only thing that closes a sale — while fixing the consent and unsubscribe plumbing that has to exist before a single non-transactional message goes out. Nothing here requires a client, a testimonial, or a metric that does not exist.

---

1. **LIFECYCLE-EMAIL-SMS-001 — Ship an email activation smoke script so turning email on is one command, not a hope.** `docs/EMAIL-DNS-AUDIT.md` step 5 says "sign up with a test address and confirm the code arrives", which tests exactly one template. Write `scripts/email-smoke.mjs` that renders every entry in `mailer.ts`'s `templates` object to `./tmp/email-preview/*.txt` in any mode, and in `live` mode sends each one to darendenchuk@gmail.com with a `[SMOKE]` subject prefix. Then activation ends with a Gmail "show original" check that dkim/spf/dmarc all pass on a real template, not just on a verification code.
impact 4/5 · effort 2/5 · touches: nevamis-engine/scripts/email-smoke.mjs (new), src/lib/mailer.ts

2. **LIFECYCLE-EMAIL-SMS-002 — Add a single CASL identification-and-unsubscribe footer builder that every commercial template must call.** CASL requires every commercial electronic message to carry the sender's name, a mailing address, one other contact method, and an unsubscribe mechanism that works for at least 60 days. Add `footer(kind: "transactional" | "commercial")` to `mailer.ts` that returns "Nevamis AI, Edmonton, Alberta · Sales@nevamis.ca · (587) 413-0035" plus, for commercial kinds only, an `https://nevamis.ca/unsubscribe.html?t=<token>` line — and make `sendEmail` throw if a template tagged commercial ships without it. One helper makes compliance structural rather than a thing you remember at 11pm.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/lib/mailer.ts

3. **LIFECYCLE-EMAIL-SMS-003 — Actually store the consent the interest form already collects.** `coming-soon.html` has a required checkbox reading "I agree Nevamis may contact me about the services I selected", but `src/app/api/interest/route.ts` parses only name/email/business/note/source/modules and drops it. Add `consent_given`, `consent_text` (the literal checkbox wording, versioned), `consent_at`, and `consent_source_url` columns to `interest_requests` and reject the POST when consent is false. Under CASL the burden of proving express consent is on the sender, and a screenshot of your own form is not proof of what a specific person agreed to on a specific day.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/app/api/interest/route.ts, src/db/schema.ts, nevamis-site/coming-soon.html

4. **LIFECYCLE-EMAIL-SMS-004 — Build `/unsubscribe.html` on the static site plus the endpoint behind it.** There is no unsubscribe page anywhere in the nevamis-site repo, which means the moment the first commercial email goes out the business is out of compliance. Build a one-field page (email address, or a signed token in the query string), styled with `assets/motion/site.css`, that POSTs to a new `app.nevamis.ca/api/unsubscribe` writing a row into `suppressions` with `tenantId: "nevamis-internal"`, `channel: "all"`, and `reason: "web unsubscribe"`. Confirm on-page immediately — CASL allows 10 business days, but honouring instantly is both easier to build and easier to defend.
impact 5/5 · effort 2/5 · touches: nevamis-site/unsubscribe.html (new), nevamis-engine/src/app/api/unsubscribe/route.ts (new), src/db/schema.ts suppressions

5. **LIFECYCLE-EMAIL-SMS-005 — Offer "pause 90 days" next to "unsubscribe" on that same page.** A trades owner who is mid-busy-season does not want to never hear from you, he wants to not hear from you this month, and unsubscribe is the only button you have given him. Add a second option that writes a `suppressed_until` date instead of a permanent suppression, and a third that narrows to "only email me when the Revenue Engine modules I picked go live". This is the cheapest way to keep a warm list warm without pretending a hard opt-out is soft.
impact 3/5 · effort 2/5 · touches: nevamis-site/unsubscribe.html, nevamis-engine/src/app/api/unsubscribe/route.ts, suppressions schema

6. **LIFECYCLE-EMAIL-SMS-006 — Write `docs/lifecycle/SEQUENCES.md` as the single source of truth for every automated touch.** Today the sequences live half in `docs/pilot/pilot-sop.md` (stages), half in `payment-email-map.md` (marked SUPERSEDED at the top), and half nowhere. One table per sequence with columns: id, trigger event, delay, channel, consent basis, stop events, and template key — using the same vocabulary as `RecipeDefinition` in `src/domain/automation.ts` so each row can be published as a recipe later without a rewrite. A solo founder cannot debug a funnel he cannot see on one page.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/lifecycle/SEQUENCES.md (new), src/domain/automation.ts

7. **LIFECYCLE-EMAIL-SMS-007 — Extend the `EmailKind` union and the `templates` object with every lifecycle moment now, while sending is still off.** `EmailKind` currently allows `verification | password_reset | booking_confirmation | pilot_summary | generic`, and four templates exist. Add the keys the sequences need (`pilot_daily_digest`, `pilot_day8_ended`, `pilot_report_ready`, `onboarding_task_due`, `monthly_value_report`, `usage_threshold`, `cancellation_confirmed`) with real plain-text bodies. The mailer's own header says the dormant path was wired "so activation needs zero code changes" — that promise only holds if the templates are written before the switch flips.
impact 4/5 · effort 3/5 · touches: nevamis-engine/src/lib/mailer.ts

8. **LIFECYCLE-EMAIL-SMS-008 — Make suppression work for people who are not yet tenants.** `isSuppressed` in `src/domain/automation.ts` filters by `tenantId` and `phoneDigits`, and `consentRecords` requires a `contactId` foreign key into `contacts` — neither shape fits a prospect on the interest list or a pilot applicant who has no tenant yet. Add a `nevamis-internal` scoped lookup path (email OR phone) and call it from any prospect-facing send. Otherwise the first person who unsubscribes will still receive the next nurture email, which is the exact failure that generates a CASL complaint.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/domain/automation.ts, src/db/schema.ts consentRecords/suppressions

9. **LIFECYCLE-EMAIL-SMS-009 — Spec the client-facing SMS adapter before writing a line of it, because the current one physically cannot do this.** `src/lib/twilio.ts` hard-wires the destination to `OWNER_CELL` by design and its header says it "can never message a client, a prospect, or any other number". Any pilot-day or onboarding SMS needs a second adapter with a consent check, a suppression check, a quiet-hours check, and an inbound webhook that turns STOP/ARRET/UNSUBSCRIBE into a `suppressions` row. Write `docs/lifecycle/SMS-ADAPTER-SPEC.md` first and keep the owner-only adapter untouched — it is the safest piece of code in the repo.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/lifecycle/SMS-ADAPTER-SPEC.md (new), src/lib/twilio.ts

10. **LIFECYCLE-EMAIL-SMS-010 — Codify Edmonton quiet hours as a testable function, not a habit.** Every lifecycle SMS and every non-urgent email should be blocked outside 08:00–21:00 Mountain, and the daily pilot digest should land at exactly 07:00 MT as `pilot-sop.md` section 11 already commits. Add `withinSendWindow(dateIso, kind)` to a new `src/lib/send-window.ts` with unit tests covering the MST/MDT boundary — `src/app/ops/day8/page.tsx` already hard-codes `UTC-6` and labels it approximate, so the DST bug is already latent in the codebase. Texting a plumber at 11pm about his usage percentage undoes a month of trust-building.
impact 3/5 · effort 2/5 · touches: nevamis-engine/src/lib/send-window.ts (new), src/app/ops/day8/page.tsx

11. **LIFECYCLE-EMAIL-SMS-011 — Make the strategy-call booking confirmation do the pre-work.** `book.html` hands off to a Cal.com iframe and Cal sends a bare calendar invite; that invite is the single highest-open-rate email in the whole funnel and it currently says nothing. Configure the Cal.com booking confirmation body to include three lines: call (587) 413-0035 before we talk, have last month's call volume roughly in mind, and know which carrier your business line is with. A prospect who has already heard the AI arrives at the call at a completely different stage.
impact 5/5 · effort 1/5 · touches: Cal.com event "nevamis-intro" confirmation template, nevamis-site/book.html

12. **LIFECYCLE-EMAIL-SMS-012 — Add a 2-hour SMS reminder to the Cal.com event and a same-day reschedule link.** Trades owners book a 15-minute call from a truck and then get a callout; a no-show is not disinterest, it is a hot water tank. Turn on Cal.com's SMS reminder for the attendee at T-2h with the reschedule link in the body, and set the workflow to also fire "we missed you — pick a new time" 30 minutes after a no-show. This is configuration inside a tool that already exists, and it recovers the most expensive lost step in the funnel.
impact 5/5 · effort 1/5 · touches: Cal.com workflows on "nevamis-intro", nevamis-site/book.html

13. **LIFECYCLE-EMAIL-SMS-013 — Send a T-24h "hear it first" nudge before every strategy call.** One email: "Before we talk tomorrow, call (587) 413-0035 and ask it to book a furnace tune-up. Takes ninety seconds. Then tell me on the call what you'd change about it." The `demo_phone_click` event already exists in `site.js`, so demo-line usage before a call is measurable, and a prospect who has used the product argues about wording instead of about whether AI can answer a phone.
impact 4/5 · effort 1/5 · touches: Cal.com workflow / manual send, nevamis-site/site.js analytics events

14. **LIFECYCLE-EMAIL-SMS-014 — Write the post-strategy-call recap email that puts the pilot terms in writing the same day.** `pilot-sop.md` stage 0 has a spoken gate script but produces no artifact, so the only record of "60 connected minutes or 30 calls, whichever comes first, no card, ends day 8" is the prospect's memory. Template it with the exact caps, the one-revision scope, the exclusions list already published on `pilot.html`, and a single "reply YES and I'll start the build" line. It doubles as the close and as the document you point at if anyone later says they expected unlimited calls.
impact 5/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/pilot-sop.md stage 0, nevamis-site/pilot.html

15. **LIFECYCLE-EMAIL-SMS-015 — Template both outcomes of the qualification gate, including the no.** `pilot-sop.md` lists six hard-fails (won't set carrier forwarding, wants number porting, expects guaranteed revenue, no human emergency escalation path, regulated vertical, shared residential line) but has no wording for delivering that verdict. Write an "accepted — here is what happens next and by when" template and a "not a fit right now, here is precisely why, here is what would change it" template. The honest no protects the founder's only scarce resource, which is build capacity, and it is the kind of email that gets forwarded to a friend who *is* a fit.
impact 4/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/pilot-sop.md

16. **LIFECYCLE-EMAIL-SMS-016 — Build a three-touch intake reminder ladder that stops the moment the form arrives.** Stage 1 of the SOP blocks the entire build on `pilot-intake-form.md` being complete, and an unreturned intake form is where a "yes" quietly dies. Send at T+1 day (email, "here's the link again, ten minutes"), T+3 (SMS if they opted in, one line), T+6 (email, "I'll hold your build slot until Friday then release it") with `stopOnEvents: ["intake.submitted"]`. Scarcity here is real — founder-led onboarding genuinely caps concurrent builds, which `pilot.html` already states.
impact 5/5 · effort 2/5 · touches: nevamis-engine/docs/lifecycle/SEQUENCES.md, docs/pilot/pilot-intake-form.md, src/domain/automation.ts

17. **LIFECYCLE-EMAIL-SMS-017 — Send the carrier forwarding codes as their own email the night before go-live, with the OFF code in the same message.** `docs/pilot/carrier-forwarding-guide.md` holds the activate/deactivate codes per carrier, but the SOP has the founder read them out on a call. An email titled "Your Telus forwarding codes — activate tonight, deactivate any time" with both codes in monospace and the ten-second undo spelled out removes the single biggest psychological barrier to going live. Put the deactivate code *first* in bold; the fear being answered is loss of control, not complexity.
impact 5/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/carrier-forwarding-guide.md

18. **LIFECYCLE-EMAIL-SMS-018 — Pre-frame the approval call by listing the eight test scenarios in the invite.** Stage 3 of the SOP runs a fixed battery: happy path, booking, emergency, excluded service, out-of-area, "are you a robot", price question, hostile caller. Put that list in the calendar invite body so the owner arrives expecting a rigorous demonstration instead of a sales pitch, and add "pick two of your own weird calls and we'll run those too". Owners who choose the test cases sign off faster and complain less afterwards.
impact 4/5 · effort 1/5 · touches: Cal.com approval-call event, nevamis-engine/docs/pilot/pilot-sop.md stage 3

19. **LIFECYCLE-EMAIL-SMS-019 — Send a go-live confirmation stating the exact start and end timestamps in Edmonton time.** `pilot.html` promises "we confirm the exact start and end time (Edmonton time) before activation", and nothing in the codebase does that. One template: forwarding confirmed live at 18:00 MT on `<date>`, pilot ends 18:00 MT on `<date+7>`, caps are 60 minutes / 30 calls, here is your deactivate code again, here is when your first digest arrives. This email is the anchor every later message references, so it is worth over-specifying.
impact 5/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, nevamis-site/pilot.html

20. **LIFECYCLE-EMAIL-SMS-020 — Turn a declined or stalled application into a consented nurture subscriber, explicitly.** When someone fails the gate or goes quiet before intake, the last email should end with a one-click "keep me posted, about once a month" link that writes a real consent record with the wording and timestamp. Do not assume the sales conversation granted ongoing marketing consent — under CASL, an inquiry gives you roughly six months of implied consent and this converts it to express while the relationship is warm. A declined prospect with recorded consent is an asset; one without is a liability.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts consentRecords, docs/lifecycle/SEQUENCES.md

21. **LIFECYCLE-EMAIL-SMS-021 — Generate the daily pilot digest instead of hand-compiling it.** `docs/pilot/daily-call-log-digest-template.md` requires the founder to pull Twilio, ElevenLabs, and Calendar data and fill a template every morning at 07:00, which is roughly 25 minutes a day and the first thing that slips on a busy week. `src/app/ops/day8/page.tsx` already runs almost exactly the right query against `calls` and `callSummaries` — clone it into `/ops/digest?day=yesterday` that renders the digest text with the `[source: …]` labels intact and a copy button. Keep the human read-every-transcript step; automate only the counting.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/app/ops/digest/page.tsx (new), src/app/ops/day8/page.tsx, docs/pilot/daily-call-log-digest-template.md

22. **LIFECYCLE-EMAIL-SMS-022 — Send a "your AI just handled its first real call" email within an hour of the first connected call.** The single most persuasive moment of the entire pilot is the first time a real stranger talks to the agent, and right now the owner learns about it in a digest the next morning. Trigger off the first `call.completed` for that tenant with a two-line summary and a link to the recording in the portal. Delight decays fast; a 40-minute-old event feels like magic and a 14-hour-old event feels like a report.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/app/api/webhooks/elevenlabs/route.ts, src/lib/mailer.ts

23. **LIFECYCLE-EMAIL-SMS-023 — Write the honest zero-call-day digest and make it a forwarding check.** A pilot day with no calls is normal for a small shop, but an empty digest reads as failure and a silent day reads as a broken product. Template: "No calls came to the pilot line yesterday. That's either a quiet day or forwarding dropped — dial `<carrier test>` to check, or I can run an external test call this morning." Naming the ambiguity yourself is what keeps the owner from quietly concluding it is broken and switching forwarding off without telling you.
impact 4/5 · effort 1/5 · touches: nevamis-engine/docs/pilot/daily-call-log-digest-template.md, src/lib/mailer.ts

24. **LIFECYCLE-EMAIL-SMS-024 — Alert at 45 minutes and 22 calls, not at the cap.** The pilot stops hard at 60 connected minutes or 30 calls and `pilot-sop.md` says "never silently exceed a cap"; hitting a ceiling with no warning turns a successful pilot into an abrupt outage. Fire a two-recipient notice at 75% of either cap — email to the owner ("you're at 45 of 60 minutes, likely to finish Thursday, want to talk earlier?") and `sendOwnerSms` to the founder. Reuse the once-per-threshold idempotency pattern already proven in `src/domain/usage.ts` (`ALERT_THRESHOLDS`, `thresholdsReached`).
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/usage.ts, src/lib/twilio.ts, src/lib/mailer.ts

25. **LIFECYCLE-EMAIL-SMS-025 — Send a day-3 one-question check-in that feeds the included revision.** Every pilot includes exactly one revision (`pilot-sop.md` stage 2), and the worst outcome is discovering on day 7 what they would have changed on day 2. One email, one question: "Reply with the one thing you'd change about how it answers." Anything that comes back becomes version 2 with a one-line changelog, which is a delivery moment as much as a fix.
impact 5/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/pilot-sop.md stage 6

26. **LIFECYCLE-EMAIL-SMS-026 — Notify the owner every time a new agent version goes live, with the changelog line.** The SOP tunes strictly by publishing versions, and each version bump is invisible to the client unless you tell them. "Version 3 is live as of 2:10pm: removed the price range on drain calls, per your note yesterday." That single email converts back-end discipline into visible responsiveness, and it is the closest thing to proof of service that exists during a free pilot.
impact 4/5 · effort 1/5 · touches: nevamis-engine/src/app/ops/agent/actions.ts, src/lib/mailer.ts

27. **LIFECYCLE-EMAIL-SMS-027 — Send a day-5 "decision preview" so day 8 is never a surprise.** Tell them exactly what the day-7 report contains (verified activity from logs, what failed and how it was fixed, a potential-value estimate built from their own average job value, a recommended plan at real pricing), and restate the two outcomes: pick a plan, or forwarding goes off and nothing is charged. Book the review call in that email. Pre-announcing the decision moment removes the ambush feeling that makes people avoid the call entirely.
impact 5/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/day7-pilot-report-template.md

28. **LIFECYCLE-EMAIL-SMS-028 — Route pilot incidents to the founder by SMS through the adapter that already works.** `sendOwnerSms` needs no consent surface because it only ever reaches `OWNER_CELL`, which makes it the right channel for kill-switch use, cap hits, a flagged emergency call, and an ElevenLabs webhook failure. Add named call sites for those four events with a 1500-character-safe body. During a live pilot the founder is the monitoring system, and he should not have to be at a laptop to be one.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/lib/twilio.ts, src/app/api/webhooks/elevenlabs/route.ts, src/domain/escalation.ts

29. **LIFECYCLE-EMAIL-SMS-029 — Email the owner the moment a booking is confirmed on the calendar, separately from the digest.** A confirmed booking is the product working end to end, and burying it in a bullet list twelve hours later wastes it. Trigger on `callSummaries.bookingConfirmed` with the caller name, service, and appointment time, subject line "Booked: Thursday 10am, drain backup, Sarah M." Over seven days this is the sequence of emails the owner will scroll back through when deciding whether to pay $449 a month.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/app/api/webhooks/elevenlabs/route.ts, src/domain/call-intake.ts, src/lib/mailer.ts

30. **LIFECYCLE-EMAIL-SMS-030 — Review the caller-facing confirmation SMS wording and its opt-out line as a template the client approves.** The product texts the *client's customers* a booking confirmation, which means the client is the sender and their CASL exposure, not just yours. Ship a fixed template — business name, appointment detail, "Reply STOP to opt out" — recorded as an `approved` row in `messageTemplates` with `category: "transactional"`, and capture the client's approval in the intake form. `docs/legal/client-responsibility-matrix.md` already splits responsibility; the template is what makes that split real.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/domain/email.ts messageTemplates, docs/pilot/pilot-intake-form.md, docs/legal/client-responsibility-matrix.md

31. **LIFECYCLE-EMAIL-SMS-031 — Schedule the 07:00 MT digest as a real recurring job instead of a personal habit.** The SOP's daily rhythm table depends entirely on the founder waking up and remembering, and the digest arriving at 07:00 on days 1–3 and 10:40 on day 4 signals fatigue louder than any wording. Use a Vercel cron hitting an authenticated route that assembles the digest and either sends it (once email is live) or texts the founder "digest ready, review and send". Consistency during a free week is the main evidence a paid month would be reliable.
impact 4/5 · effort 2/5 · touches: nevamis-engine/vercel.json cron, src/app/ops/digest, docs/pilot/pilot-sop.md section 11

32. **LIFECYCLE-EMAIL-SMS-032 — Wire `docs/CRISIS-COMMS-TEMPLATES.md` into the lifecycle as sendable templates with a hard 60-minute rule.** A wrong answer to a real caller during a pilot is survivable if the client hears it from you first and unsurvivable if he hears it from his customer. Convert the crisis doc into `mailer.ts` templates for the three realistic cases (agent gave unapproved information, agent mishandled an emergency, line was down) with the pattern: what happened, which calls were affected, what was changed, what you get. Add the 60-minute disclosure commitment to `docs/pilot/pilot-sop.md` section 7.6 so it is policy rather than instinct.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/CRISIS-COMMS-TEMPLATES.md, src/lib/mailer.ts, docs/pilot/pilot-sop.md

33. **LIFECYCLE-EMAIL-SMS-033 — Deliver the day-7 report 24 hours before the decision call, not on it.** `day7-pilot-report-template.md` is currently walked through live, which means the owner is reading and deciding simultaneously and will default to "let me think about it". Send it the evening before with "read the first table, that's the whole story — we'll talk tomorrow at 10". An owner who has already absorbed the numbers spends the call negotiating terms instead of processing information.
impact 5/5 · effort 1/5 · touches: nevamis-engine/docs/pilot/day7-pilot-report-template.md, src/app/ops/day8/page.tsx, src/lib/mailer.ts

34. **LIFECYCLE-EMAIL-SMS-034 — Build the day-8 "it ended, nothing was charged" email and send it even when they go silent.** `pilot.html` promises "On day eight it ends unless you say otherwise" and "Silence never becomes a subscription", and there is currently no mechanism that honours that in writing. Template: forwarding is off (confirmed by external test at `<time>`), no card was ever taken, nothing renews, your data is handled as follows, here is your report if you want it later. Sending this to someone who ghosted you is the highest-integrity email in the whole business and the one most likely to bring them back in November.
impact 5/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/pilot-end-teardown-checklist.md, nevamis-site/pilot.html

35. **LIFECYCLE-EMAIL-SMS-035 — Give "undecided" a real three-touch branch with a stated end.** The day-7 report has an explicit ☐ Undecided box and nothing behind it. Follow up on day 10 (the one number from their own pilot that mattered most), day 17 (an answer to the specific objection they raised, drawn from `outreach/OBJECTION-CARDS.md`), and day 31 ("I'm closing your file — say the word any time and the config is still there"). Three touches, then stop, and say so in the first one; open-ended pestering is what turns a maybe into a block.
impact 5/5 · effort 2/5 · touches: nevamis-engine/docs/lifecycle/SEQUENCES.md, ai-assistant/outreach/OBJECTION-CARDS.md

36. **LIFECYCLE-EMAIL-SMS-036 — Write the acceptance email that starts billing, and make written acceptance a reply, not a signature ceremony.** The SOP is emphatic that billing begins only after explicit written acceptance. Template it with the chosen plan rendered from `pricing-config.js` values (e.g. Growth C$449/mo + C$750 setup, 600 minutes), the annual prepay alternative (C$4,490 for twelve months), the founding-client waiver if it applies, the GST line, and one instruction: "reply with the word ACCEPT and the plan name." Friction at the yes is the most expensive friction in the business.
impact 5/5 · effort 2/5 · touches: nevamis-site/pricing-config.js, nevamis-engine/src/lib/mailer.ts, docs/pilot/pilot-end-teardown-checklist.md

37. **LIFECYCLE-EMAIL-SMS-037 — Fix the GST contradiction before any money email goes out.** `docs/pilot/day7-pilot-report-template.md` section 5 and `docs/pilot/pilot-sop.md` section 0 both state "not GST/HST registered — do not add GST", while the business is registered as 705729200 RT0001. Whichever is current, every quote, acceptance, invoice, and receipt template must state it identically and show the registration number when GST is charged. A pilot report that says "no GST" followed by an invoice that adds 5% is the kind of small inconsistency that makes a cautious owner re-read the whole agreement.
impact 5/5 · effort 1/5 · touches: nevamis-engine/docs/pilot/day7-pilot-report-template.md, docs/pilot/pilot-sop.md, src/lib/mailer.ts, src/domain/billing.ts

38. **LIFECYCLE-EMAIL-SMS-038 — Template the founding-client offer so the waiver is never entangled with the review ask.** The offer is a real setup-fee waiver (C$500/C$750/C$1,250 depending on plan) for the first five who continue, in exchange for feedback and *permission to ask* for a review. Write it as two clearly separated paragraphs — the waiver is unconditional on continuing; the review is a separate request you may decline — and state the count honestly ("you'd be one of the first five"). Conditioning a discount on a positive review is a Competition Act problem and a trust problem, and separating them in the template makes it structurally impossible to blur on a call.
impact 4/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, docs/pilot/day7-pilot-report-template.md

39. **LIFECYCLE-EMAIL-SMS-039 — Send a data-handling email at pilot end that matches what the systems actually do.** `docs/DATA-RETENTION.md` is admirably honest that nothing auto-deletes today and that the ElevenLabs fixed retention window is not configured yet. The end-of-pilot email should say exactly that: recordings and transcripts live with our voice provider and are deleted on request within X days, call summaries can be exported to CSV for you first, here is the email address to request deletion. Promising deletion you have not built is the one lie that would actually be actionable.
impact 4/5 · effort 1/5 · touches: nevamis-engine/docs/DATA-RETENTION.md, nevamis-site/privacy.html, src/lib/mailer.ts

40. **LIFECYCLE-EMAIL-SMS-040 — Capture the loss reason in one click inside the "not now" email.** After a no, send a two-line email with five mailto links whose subject lines are the reasons — too expensive, not enough calls to justify it, want to see it running longer, prefer a human answering service, bad timing. Log each into `outreach/prospect-tracker.csv` and a `loss_reason` field. With zero clients, the loss reasons from the first ten pilots are worth more than any analytics dashboard, and a mailto link is the only reply mechanism with a near-100% technical success rate.
impact 4/5 · effort 1/5 · touches: ai-assistant/outreach/prospect-tracker.csv, nevamis-engine/src/lib/mailer.ts

41. **LIFECYCLE-EMAIL-SMS-041 — Build a 30-day post-pilot win-back that quotes their own pilot numbers back to them.** Thirty days after a "not now", one email: "Your week caught 9 calls after 5pm, 3 of them asked to book. That was one week in July. Want the config switched back on?" It uses only verified data from their own pilot, requires no new claims, and lands after they have had a month of missing those calls again. Store the pilot summary at teardown specifically so this email can be written a month later without digging through logs.
impact 5/5 · effort 2/5 · touches: nevamis-engine/docs/pilot/pilot-end-teardown-checklist.md, src/app/ops/day8/page.tsx, docs/lifecycle/SEQUENCES.md

42. **LIFECYCLE-EMAIL-SMS-042 — Trigger seasonal win-backs off the trade, not off a fixed calendar.** An HVAC owner who said no in July is a different person during the first −25°C week in November; a restoration company is a different person during spring melt; a roofer is a different person the day after hail. Add a `seasonal_trigger` value per lost prospect and one scheduled email per trigger that opens with the weather, not with Nevamis. This costs one template and turns dead rows into the warmest list the business has.
impact 5/5 · effort 2/5 · touches: ai-assistant/outreach/prospect-tracker.csv, nevamis-engine/docs/lifecycle/SEQUENCES.md

43. **LIFECYCLE-EMAIL-SMS-043 — Turn "call me in three months" into a dated obligation with a pre-written email.** Right now that promise lives in the founder's head and in a CSV note, which is where follow-ups go to die. When a prospect names a timeframe, immediately create the scheduled task *and* draft the email that will send on that date, referencing what they said ("you mentioned you'd revisit after the shop move"). Prospects consistently reward the person who remembered the specific thing they said, and drafting it while the conversation is fresh takes ninety seconds.
impact 4/5 · effort 1/5 · touches: ai-assistant/outreach/prospect-tracker.csv, nevamis-engine/src/app/ops/queue

44. **LIFECYCLE-EMAIL-SMS-044 — Give the coming-soon interest list one monthly build note, and only to people who checked the box.** `interest_requests` stores a `modules_json` array of exactly which Revenue Engine modules each person asked for, which is a segmentation gift nobody is using. Send one short email a month: what shipped, what is next, and one line specific to the modules they selected. Everything on `coming-soon.html` must stay labelled "coming soon" in these emails — the value is proof of momentum, not a pre-sale.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/app/api/interest/route.ts, src/app/ops/interest, nevamis-site/coming-soon.html

45. **LIFECYCLE-EMAIL-SMS-045 — Record consent *basis* and compute an expiry date, not just a boolean.** `consentRecords.basis` already supports `express | implied_inquiry | existing_relationship`, which is exactly the CASL distinction, but nothing computes when implied consent lapses — six months from an inquiry, two years from an existing business relationship. Add a derived `expires_at` and make any nurture query filter on it. Without this, the list silently rots into non-compliance at exactly the moment it gets big enough to matter.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts consentRecords, src/domain/email.ts marketingSendAllowed

46. **LIFECYCLE-EMAIL-SMS-046 — Send a re-consent email 30 days before implied consent expires.** One email to anyone approaching the six-month inquiry window: "You asked about Nevamis in March. I can keep sending occasional updates, but only if you say so — one click either way." Both buttons write a record, and the silence branch suppresses automatically. This is the rare compliance requirement that also improves list quality, because it deletes the people who were never going to reply anyway.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/db/schema.ts consentRecords, nevamis-site/unsubscribe.html, docs/lifecycle/SEQUENCES.md

47. **LIFECYCLE-EMAIL-SMS-047 — Feed the nurture stream from artefacts that already exist rather than commissioning new content.** `outreach/ROI-ONE-PAGER.md`, `VERTICAL-FAQ-TRADES.md`, `OBJECTION-CARDS.md`, and `CALL-LOG-SAMPLE.md` are already written and already honest; `docs/service-blueprints/*.md` describes ten future modules in plain language. Map one of these to each monthly nurture slot in `SEQUENCES.md` so six months of email exists today. The failure mode for a solo founder is not bad content, it is an empty calendar slot at 9pm on a Sunday.
impact 4/5 · effort 1/5 · touches: ai-assistant/outreach/*.md, nevamis-engine/docs/service-blueprints/, docs/lifecycle/SEQUENCES.md

48. **LIFECYCLE-EMAIL-SMS-048 — State the frequency promise in the first nurture email and never exceed it.** "One email a month, usually short, unsubscribe at the bottom of every one" in the opening paragraph sets an expectation you can actually keep at current capacity, and it dramatically lowers the unsubscribe rate on email two. Put the same sentence in the `coming-soon.html` form note next to the consent checkbox so the promise is made before the address is given. Under-promising cadence is free and permanently compounding.
impact 3/5 · effort 1/5 · touches: nevamis-site/coming-soon.html, nevamis-engine/src/lib/mailer.ts

49. **LIFECYCLE-EMAIL-SMS-049 — Drive onboarding emails off the real task state that already exists in the database.** `src/domain/onboarding.ts` builds a six-task plan per client with `ownerSide: "client" | "nevamis"`, dependency unblocking, and a readiness gate — every state change there is an email trigger nobody has connected. Send on task-unblocked ("your calendar connection is the only thing between you and a test call"), on staff-set blocker, and on readiness reached. The data model is finished; only the notification layer is missing.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/onboarding.ts, src/lib/mailer.ts

50. **LIFECYCLE-EMAIL-SMS-050 — Chase the first portal login 48 hours after the workspace email.** `templates.workspaceLinked` already tells a new client their portal is ready and points them at the "Your business" page, and nothing happens if they never open it. Check for a `sessions` row for that user and send a second, shorter nudge with a direct deep link and "reply and I'll fill it in with you on a ten-minute call". Trades owners are not portal people, and the fallback of doing it for them on a call should be offered early rather than after a week of silence.
impact 4/5 · effort 2/5 · touches: nevamis-engine/src/lib/mailer.ts, src/app/ops/accounts/actions.ts, src/db/schema.ts sessions

51. **LIFECYCLE-EMAIL-SMS-051 — Make the client acceptance sign-off an email with fixed wording, since it is recorded as a legal-ish approval.** `approveTask` writes a `clientApprovals` row with the approver's name and an optional note, but the client's actual words come from a phone call. Send a template that lists what they are approving (greeting wording, emergency rule, booking rule, recording setting) and asks them to reply "approved" — then paste that reply into the `note` field. The audit trail becomes something you could show counsel rather than a name and a timestamp.
impact 4/5 · effort 1/5 · touches: nevamis-engine/src/domain/onboarding.ts approveTask, src/lib/mailer.ts

52. **LIFECYCLE-EMAIL-SMS-052 — Pair the go-live email with a day-7 "first week" check-in for paying clients.** `goLive` in `src/domain/onboarding.ts` flips the project to launched and tells nobody. Send the launch email (what changed, what to watch for, how to reach a human) and schedule a check-in seven days later with the week's real numbers from `portal-metrics.ts` and one question: "what's the first thing you'd change?" The first paid week is where churn is decided, and it is the cheapest week to intervene in.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/onboarding.ts goLive, src/domain/portal-metrics.ts, src/lib/mailer.ts

53. **LIFECYCLE-EMAIL-SMS-053 — Write an escalation rule that converts two unanswered onboarding emails into a phone call.** Email is the wrong channel for a busy trades owner and pretending otherwise is how onboarding projects stall at 60%. Rule in `SEQUENCES.md`: after two emails on the same blocked task with no reply, the sequence stops and creates a "call `<name>` about `<task>`" item in the ops action queue with the phone number attached. Knowing when to stop emailing is a lifecycle design decision, not a failure of the sequence.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/lifecycle/SEQUENCES.md, src/domain/queue.ts, src/domain/onboarding.ts

54. **LIFECYCLE-EMAIL-SMS-054 — Ask every client at intake how they want to be reached, and honour it.** Add three fields to `docs/pilot/pilot-intake-form.md`: preferred channel for daily digests (email / SMS / both), best time of day, and who else should receive owner summaries. Store them and branch the digest and alert sequences on them. A partner who wants the 7am digest by text and the monthly report by email is telling you exactly how to keep him, and asking costs one line on a form he is already filling out.
impact 4/5 · effort 1/5 · touches: nevamis-engine/docs/pilot/pilot-intake-form.md, src/db/schema.ts, src/domain/profile.ts

55. **LIFECYCLE-EMAIL-SMS-055 — Ship a monthly value report email built only from that client's real portal metrics.** `src/domain/portal-metrics.ts` and `src/lib/weekly.ts` already compute the numbers the performance dashboard shows; the report is the same figures in an email on the 1st with a link to the dashboard and a CSV export link (`/api/export/calls?month=…` already exists). Report calls answered, after-hours share, bookings confirmed, minutes used against the plan allowance — and no dollar figure presented as earned revenue, matching the rule already enforced in the daily digest. This is the artifact that makes a $449 line item on a credit card statement feel obviously worth it.
impact 5/5 · effort 3/5 · touches: nevamis-engine/src/domain/portal-metrics.ts, src/lib/weekly.ts, src/app/api/export/calls/route.ts, src/lib/mailer.ts

56. **LIFECYCLE-EMAIL-SMS-056 — Turn the existing usage alert thresholds into emails with a plan-change path at 90%.** `src/domain/usage.ts` already records 50/75/90/100% crossings idempotently per period and computes `estimatedOverageCents`, but nothing reaches the client. At 75% send an informational note; at 90% send "at this pace you'll finish the month around X minutes over, which is about $Y at your overage rate — moving to Growth would cost less"; at 100% state exactly what happens next. A billing surprise at month end is the single most avoidable cancellation cause and the mechanics are already built.
impact 5/5 · effort 2/5 · touches: nevamis-engine/src/domain/usage.ts, src/lib/mailer.ts, nevamis-site/pricing-config.js

57. **LIFECYCLE-EMAIL-SMS-057 — Send an annual prepay offer at month three, with the arithmetic shown.** `pricing-config.js` prices annual prepay at ten months for twelve (2490 / 4490 / 8490), which is a C$898 saving on Growth that no client will discover on their own. After three clean months, email the actual numbers: what they have paid so far, what twelve months costs monthly versus prepaid, and a single yes/no reply. Prepay converts a churn-risk month into a year of cash and is the highest-leverage email a business with no clients can pre-write today.
impact 4/5 · effort 1/5 · touches: nevamis-site/pricing-config.js, nevamis-engine/src/domain/billing.ts, src/lib/mailer.ts

58. **LIFECYCLE-EMAIL-SMS-058 — Time the referral ask to a specific booked job, not to a calendar date.** `outreach/REFERRAL-OFFER.md` exists but is still marked DRAFT and has no trigger. Fire the ask after a month in which the client had at least one confirmed booking that came in outside business hours, and open with that specific call: "the 9:40pm water heater call on the 14th — who else in your trade is missing those?" Referrals from Edmonton trades owners to other Edmonton trades owners are the most plausible path to clients two through five, and specificity is what makes the ask answerable.
impact 5/5 · effort 2/5 · touches: ai-assistant/outreach/REFERRAL-OFFER.md, nevamis-engine/src/domain/portal-metrics.ts, src/lib/mailer.ts

59. **LIFECYCLE-EMAIL-SMS-059 — Write the review request as a genuinely declinable ask, sent separately from anything financial.** The founding-client terms grant permission to *ask* for a review, and the ask must never arrive in the same email as a waiver, an invoice, or a discount. Send it standalone after a positive month with a direct Google Business Profile link, an explicit "a no costs you nothing and changes nothing", and no template text for them to paste. The site has zero testimonials today and must keep having zero until a real client writes a real one — this email is the only honest route to the first.
impact 4/5 · effort 1/5 · touches: nevamis-engine/src/lib/mailer.ts, ai-assistant/GBP-DECISION.md

60. **LIFECYCLE-EMAIL-SMS-060 — Build the cancellation sequence so leaving is clean and the door stays open.** `docs/OFFBOARDING-SOP.md` covers the operational side; the communication side needs four messages: confirmation with the exact service end date and final charge, a one-question exit reason, a data export link plus deletion timeline matching `DATA-RETENTION.md`, and confirmation that forwarding is off and their number is untouched. Then a single win-back at 60 days. A client who offboards without one surprise is a referral source and a possible return; one who has to chase you for his data is a review problem.
impact 4/5 · effort 2/5 · touches: nevamis-engine/docs/OFFBOARDING-SOP.md, docs/DATA-RETENTION.md, src/domain/billing.ts, src/lib/mailer.ts
