{
  "name": "nevamis-site-motion-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only motion proof harness for the Nevamis static site. Not part of the published site (see _config.yml exclude + .gitignore).",
  "scripts": {
    "serve": "node serve.js",
    "proof": "playwright test",
    "check": "node scripts/check-all.mjs",
    "consistency": "node scripts/check-consistency.js",
    "audit": "node scripts/audit-site.mjs",
    "suite": "node scripts/check-suite.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
